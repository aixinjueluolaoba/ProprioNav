# 盲人寻路强化学习实验记录

更新日期：2026-05-21

这个文件用于记录已经尝试过的环境、状态、动作、算法、结果和回退入口。以后改坏了，优先从这里找上一条可用路线。

## 2026-05-21 v11 宏动作库实验

目的：减少 v10 在高障碍/内凹山体附近反复 recovery 后表现出的转圈。当前判断不是单纯训练不足，而是固定两段式 recovery 在盲观测下容易被碰撞滑移和 heading 锚定放大成绕圈。

改动：

- 新增底层 action mode：`heading_relative_macro_library_recovery`
- 新增 state mode：`observable12_target8_macro_library_v11_stage1/stage2a/stage2/stage3`
- 动作空间：`MultiDiscrete([5, 2, 7])`
- 第 3 维宏动作：`normal / short_back / back_left / back_right / wide_left / wide_right / back_then_realign`

宏动作库：

| 宏动作 | 模板 |
|---|---|
| `short_back` | 后退 2 step |
| `back_left` | 后退 2 step + 左移 2 step |
| `back_right` | 后退 2 step + 右移 2 step |
| `wide_left` | 后退 2 step + 左移 4 step |
| `wide_right` | 后退 2 step + 右移 4 step |
| `back_then_realign` | 后退 2 step + 朝目标方向重对齐 2 step |

验证：

- 远端 `v1002` / `ML` 环境 `py_compile` 通过。
- 四个 v11 stage smoke 通过：`obs.shape = (12,)`，`action_space = MultiDiscrete([5 2 7])`。
- 强制打开 selective gate 后，`macro_id 1..6` 均能触发，对应剩余步数分别为短/中/长模板。
- 训练入口 smoke 通过。

当前短训：

- 启动脚本：`tools/启动_v11宏动作库短训_v1002.sh`
- 训练目录：`/home/diana/fishing/blind_nav_rl/runs/目标8宏动作库v11短训_v1002`
- 远端代码目录：`/home/diana/fishing/blind_nav_rl_reward_code_20260521`
- 训练配置：`stage1=2000, stage2a=1000, stage2=2000, stage3=1000, n_envs=96`
- 已确认 `stage1 checkpoint_ep_00500.zip` 产出。

后续评估口径：

- 等短训完整跑完后，评估普通/压力/对角高障碍三组。
- 重点不只看成功率，还要看 `recovery_action_rate`、碰撞数、路径效率、最终角度误差，以及视频里是否减少绕圈。

## 2026-05-21 v11b 当前最佳归档

当前已确认把 `v11b stage2 checkpoint_ep_01000.zip` 作为默认交付 checkpoint。

- 模型路径：
  `/home/diana/fishing/blind_nav_rl/runs/目标8宏动作库v11b卡住探索短训_v1002/rppo_medium_lstm128x2_observable12_target8_macro_library_v11_stage2/models/checkpoint_ep_01000.zip`
- watcher 汇总：
  `/home/diana/fishing/blind_nav_rl/runs/目标8宏动作库v11b_checkpoint评估_v1002/checkpoint_eval_summary.csv`
- 选择原因：
  - `stage2 01000 / 01500 / 02000` 指标基本持平
  - `01000` 是最早达到当前最佳水平的 checkpoint
  - `stage3` 普通步数更差，pressure 也未超过 `stage2`

最终 20 局归档结果：

| 场景 | 成功率 | 平均步数 | 平均碰撞 | 终点角度误差 | 末 5 步角度误差 |
|---|---:|---:|---:|---:|---:|
| 普通评估 20 轮 | `1.00` | `19.90` | `2.95` | `0.00006deg` | `0.00002deg` |
| 压力评估 20 轮 | `1.00` | `31.55` | `8.15` | `0.00006deg` | `0.00002deg` |
| 对角高障碍 20 轮 | `1.00` | `75.70` | `12.90` | `0.00011deg` | `0.11806deg` |

归档目录：

- 远端运行归档：
  `/home/diana/fishing/blind_nav_rl/runs/目标8宏动作库v11b当前最佳归档_v1002`
- 本地分享目录：
  `/home/diana/screencap/file/目标8宏动作库v11b当前最佳成果`

预览链接：

- ASCII 主入口预览：
  `http://47.99.153.111:12346/file/target8-best/preview.html`
- ASCII 主入口清单：
  `http://47.99.153.111:12346/file/target8-best/manifest.json`
- 本地预览：
  `http://127.0.0.1:6000/file/%E7%9B%AE%E6%A0%878%E5%AE%8F%E5%8A%A8%E4%BD%9C%E5%BA%93v11b%E5%BD%93%E5%89%8D%E6%9C%80%E4%BD%B3%E6%88%90%E6%9E%9C/%E6%88%90%E6%9E%9C%E9%A2%84%E8%A7%88.html`
- 稳定短链接预览：
  `http://47.99.153.111:12346/file/%E7%9B%AE%E6%A0%878%E5%BD%93%E5%89%8D%E6%9C%80%E4%BD%B3%E6%88%90%E6%9E%9C/%E6%88%90%E6%9E%9C%E9%A2%84%E8%A7%88.html`
- 稳定短链接清单：
  `http://47.99.153.111:12346/file/%E7%9B%AE%E6%A0%878%E5%BD%93%E5%89%8D%E6%9C%80%E4%BD%B3%E6%88%90%E6%9E%9C/%E5%88%86%E4%BA%AB%E6%B8%85%E5%8D%95.json`
- 已验证外网直连：
  `http://47.99.153.111:12346/file/%E7%9B%AE%E6%A0%878%E5%AE%8F%E5%8A%A8%E4%BD%9C%E5%BA%93v11b%E5%BD%93%E5%89%8D%E6%9C%80%E4%BD%B3%E6%88%90%E6%9E%9C/%E6%88%90%E6%9E%9C%E9%A2%84%E8%A7%88.html`
- 代理候选：
  `http://ai.jiaran.icu:12346/file/%E7%9B%AE%E6%A0%878%E5%AE%8F%E5%8A%A8%E4%BD%9C%E5%BA%93v11b%E5%BD%93%E5%89%8D%E6%9C%80%E4%BD%B3%E6%88%90%E6%9E%9C/%E6%88%90%E6%9E%9C%E9%A2%84%E8%A7%88.html`
- 服务自报公开基址：
  `http://47.99.153.111:12346/file`
- 稳定短目录：
  `/home/diana/screencap/file/目标8当前最佳成果`
- ASCII 稳定短目录：
  `/home/diana/screencap/file/target8-best`

连通性说明：

- 本地 `127.0.0.1:6000/file/...` 已验证 `HTML` 和 `MP4` 返回 `200`
- 直连 `47.99.153.111:12346/file/...` 已验证 `HTML` 返回 `200`
- Playwright 浏览器上下文访问同一 IP URL 也正常打开成果页
- 当前 shell 直接 `curl` 代理候选返回 `403`
- Playwright 浏览器上下文访问同一代理候选也返回 `403`，页面标题为 `Non-compliance ICP Filing`
- `http://47.99.153.111:12346/` 会直接命中当前本机 `uvicorn`，说明 `frpc` 的 `6000 -> 12346` 隧道是通的
- `screencap-dataset-ui` 已改为支持 `PUBLIC_FILE_BASE` 覆盖，当前 `run.sh` 默认导出 `http://47.99.153.111:12346/file`
- 本地请求 `/api/files` 时，当前自报 `public_base = http://47.99.153.111:12346/file`
- 远端归档脚本和本地同步脚本都已生成稳定短目录 `目标8当前最佳成果`，包含 `成果预览.html`、`链接清单.md`、`分享清单.json`
- 远端归档脚本和本地同步脚本都已生成 ASCII 短目录 `target8-best`，包含 `preview.html`、`links.md`、`manifest.json`、`normal.mp4`、`pressure.mp4`、`diagonal.mp4`
- 当前结论已升级为：已验证可用的公网文件出口是 `47.99.153.111:12346/file/...`，域名 `ai.jiaran.icu:12346` 的 Host/边缘策略仍会返回 `403`

脚本收口补充：

- `tools/打印_v11b当前最佳checkpoint_v1002.sh` 已改为从 watcher 汇总自动选当前最佳，不再写死 `stage2 01000`
- `tools/归档_v11b当前最佳_v1002.sh` 已改为同样的自动选优逻辑，归档文件名会随当前最佳自动切换
- 新增 `tools/同步并归档_v11b当前最佳_v1002.sh`，以后重跑远端归档时不需要再手动 `scp` 归档脚本
- `tools/同步_v11b当前最佳成果到本地分享.sh` 已改为从本地 `/api/files` 动态读取 `public_base`
- 新增 `tools/诊断_v11b分享链路.sh`，用于同时诊断本地路由、代理候选和服务自报公开基址
- `screencap-dataset-ui/app.py` 已去掉硬编码 `43.242.75.250:12346/file`，改为优先读环境变量，否则按当前请求动态生成

## 2026-05-21 指标口径修正

用户真实角度目标：到达目标时朝向误差尽量低；全轨迹平均角度误差只用于约束路径不要太曲折，不能作为最终角度主指标。

已补评估字段：

| 字段 | 含义 | 用途 |
|---|---|---|
| `avg_final_abs_angle_error_deg` | 每局结束/到达时的绝对朝向误差均值 | 核心角度指标 |
| `avg_last5_abs_angle_error_deg` | 每局最后 5 个状态的绝对朝向误差均值 | 判断末段是否稳定对准 |
| `avg_abs_angle_error_deg` | 全轨迹逐步绝对朝向误差均值 | 只作路径质量参考 |

已更新：

- `eval_target8_sweep.py`
- `eval_target8_pressure.py`
- `watch_v10_checkpoints.py`
- `watch_v10b9_checkpoints.py`

## 2026-05-21 v10b9 九方向动作分支

目的：保留 `v10` 的 12D 盲状态、显式 recovery 和 curriculum，把方向动作从 `5` 桶扩到 `9` 桶，验证更细末段调向是否改善终点/末段角度。

代码接线：

| 项 | 状态 |
|---|---|
| `observable12_target8_discrete_recovery_v10b9_stage1/stage2a/stage2/stage3` | 已加入 |
| `Observable12DiscreteRecoveryV10B9Wrapper` | 已加入 |
| 动作空间 | `MultiDiscrete([9, 2, 4])` |
| 训练入口 | `train_target8_v10b9_curriculum.py` |
| watcher | `watch_v10b9_checkpoints.py` |
| pressure eval | `eval_target8_pressure.py --action-mode v10b9` |

验证：

- 本地 `py_compile` 通过。
- 本地 smoke：`action_space = MultiDiscrete([9 2 4])`，`obs.shape = (12,)`。
- v1002 codecopy smoke：`action_space = MultiDiscrete([9 2 4])`，`obs.shape = (12,)`。

短训计划：

| 项 | 值 |
|---|---|
| 代码副本 | `/home/diana/fishing/blind_nav_rl_reward_code_20260521` |
| 训练目录 | `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10b9短训_v1002` |
| checkpoint 评估目录 | `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10b9短训_checkpoint评估_v1002` |
| 启动脚本 | `tools/启动_v10b9短训_v1002.sh` |
| stage1/stage2a/stage2/stage3 | `2000 / 1000 / 2000 / 1000` |
| 并行环境 | `96` |
| checkpoint 间隔 | `500` |

注意：`v10A/AE500` checkpoint 不能直接 resume 到 `v10b9`，因为动作头从 `[5,2,4]` 变为 `[9,2,4]`。

### v10A vs AE500 新角度口径复评

复评目录：`/home/diana/fishing/blind_nav_rl/runs/目标8_v10A_AE500_终点角度复评_v1002`

Normal/Pressure 各 50 局，新增指标：

| 模型 | Normal success | Normal avg steps | Normal final angle | Normal last5 angle | Pressure success | Pressure avg steps | Pressure final angle | Pressure last5 angle | Pressure recovery | Pressure escape | Pressure collisions |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| v10A `checkpoint_ep_01500` | `1.00` | `39.66` | `0.00007deg` | `0.00002deg` | `0.90` | `59.24` | `10.74deg` | `8.03deg` | `0.386` | `0.92` | `11.64` |
| AE500 `checkpoint_ep_00500` | `1.00` | `39.40` | `0.00007deg` | `0.00002deg` | `0.94` | `49.68` | `5.35deg` | `4.35deg` | `0.350` | `0.92` | `9.70` |

结论：

- Normal 到达终点时两者朝向都几乎为 `0deg`，旧的全轨迹角度误差不应作为 Normal 终点准度问题处理。
- Pressure 下 `AE500` 在成功率、步数、终点角度、末 5 步角度、碰撞上都优于 `v10A`，且 escape rate 相同。
- 以当前“到达目标时误差尽量低”为目标，已有 checkpoint 中 `AE500` 暂时领先；`v10A` 仍是稳定 recipe 回退线。

### v10b9 首个 checkpoint 观察

`v10b9 stage1 checkpoint_ep_00500` 自动评估结果：

| checkpoint | Normal success | Normal avg angle | Normal final angle | Pressure success | Pressure avg angle | Pressure final angle | Pressure recovery | Pressure escape | Pressure collisions |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| stage1 `00500` | `0.90` | `6.30deg` | `0.00004deg` | `0.85` | `6.04deg` | `0.07deg` | `0.000` | `0.00` | `43.40` |

早期判断：

- `v10b9` 的 9 方向动作能把全轨迹和终点角度压得很低。
- 但 stage1 不启用 recovery，pressure 下碰撞很高、escape 为 `0`，不能作为候选。
- 继续等待后续 stage2a/stage2/stage3，重点看 recovery 是否恢复，同时保持末段角度优势。

### v10b9 stage2a00500 放大复评

复评目录：`/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10b9_stage2a00500_大样本复评_v1002`

对 `stage2a checkpoint_ep_00500` 做 normal/pressure 各 50 局复评，并与 `AE500` 同口径比较：

| 模型 | Normal success | Normal steps | Normal final angle | Normal collisions | Pressure success | Pressure steps | Pressure final angle | Pressure last5 angle | Pressure recovery | Pressure escape | Pressure collisions |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| AE500 | `1.00` | `39.40` | `0.00007deg` | `5.44` | `0.94` | `49.68` | `5.35deg` | `4.35deg` | `0.350` | `0.92` | `9.70` |
| v10b9 stage2a00500 | `1.00` | `36.98` | `0.00005deg` | `1.88` | `0.94` | `63.26` | `5.87deg` | `5.34deg` | `0.276` | `0.96` | `10.44` |

结论：

- `v10b9 stage2a00500` 在 normal 步数和碰撞上更好，说明 9 方向动作有改善轨迹质量的潜力。
- 但按用户主目标“到达目标时误差尽量低”，pressure 终点角度和末 5 步角度仍不如 `AE500`，pressure 步数/碰撞也略差。
- 因此当前不能替代 `AE500`；继续等待 `stage3`，若没有新的 pressure 终点角优势，当前推荐仍是 `AE500`。

## 2026-05-21 调度原则

用户目标：继续尝试多种方案，允许换网络、状态、动作、环境，在 `v1002` 和 `253` 同时跑实验；每个版本必须记录，保证可回退，尽量少占用 `253` 硬盘，最终选出最好的模型。

当前约束：

- `253` 根分区约 `98G`，已用约 `64G`，剩余约 `30G`；后续只放短训、少视频、少 checkpoint 的实验。
- `v1002` 根分区约 `734G`，剩余约 `130G`；优先承接新方案批量训练。
- 所有新批次必须有独立 `RUN_GROUP_NAME`、独立 `configs/*.json`、独立 `批次参数.csv`，避免覆盖旧结果。
- 默认不删除远端旧模型；需要清理时先拉回汇总、模型和视频，再定向清理对应 run 目录。

### v10A 角度误差改进批次

背景：

- `v10 方案A` 普通 20 局成功率 `1.00`，平均步数 `36.45`，平均角度误差 `34.67deg`。
- 压力 20 局成功率 `0.95`，平均步数 `46.65`，平均角度误差 `40.55deg`，recovery escape rate `0.90`。
- 之前 `P/Q` 奖励压角分支能把普通/压力角度误差降到约 `10.53deg / 8.02deg`，但 recovery rate 退化为 `0.0`，碰撞约 `31.1`，不适合作为最终方案。

新增批次目标：

- 保留 `方案A` 的 selective recovery gate，不破坏已有脱困稳定性。
- 只在普通移动段轻压角度，或把正常方向范围从 `±60deg` 缩到 `±45deg`。
- 不改变 action space 形状，继续可从 `stage2 checkpoint 03000` 回退/续训。

新增配置：

| 方案 | 文件 | 改动 | 预期 |
|---|---|---|---|
| V | `configs/v10_stage3/方案V_A收紧gate轻压角.json` | A gate + 非 recovery 段轻压角度/转向/动作变化 | 降低角度误差，尽量保留 recovery |
| W | `configs/v10_stage3/方案W_A收紧gate45度收角.json` | A gate + `delta_angle_limit=45deg` | 降低普通段折线和过冲 |
| X | `configs/v10_stage3/方案X_A收紧gate45度轻压角.json` | W + 轻压非 recovery 角度 | 检查两种压角是否可叠加 |
| Y | `configs/v10_stage3/方案Y_A收紧gate全局轻压角.json` | A gate + 远端兼容的全局轻压角/转向/动作变化 | 兼容旧远端代码的 V 替代 |
| Z | `configs/v10_stage3/方案Z_A收紧gate45度全局轻压角.json` | W + 远端兼容的全局轻压角 | 兼容旧远端代码的 X 替代 |
| AA | `configs/v10_stage3/方案AA_A收紧gate50度收角.json` | A gate + `delta_angle_limit=50deg` | 45deg 过窄时的中间回退 |
| AB | `configs/v10_stage3/方案AB_A收紧gate55度收角.json` | A gate + `delta_angle_limit=55deg` | 接近原始 60deg 的轻收角 |
| AC | `configs/v10_stage3/方案AC_A收紧gate40度收角.json` | A gate + `delta_angle_limit=40deg` | 比 45deg 更强的收角 |
| AD | `configs/v10_stage3/方案AD_A收紧gate35度收角.json` | A gate + `delta_angle_limit=35deg` | 检查收角过窄是否损伤通过率 |

启动入口：

- `tools/启动_v10阶段3方案VWX角度对照_v1002.sh`
- `tools/启动_v10阶段3方案YZAA角度对照_v1002.sh`
- `tools/启动_v10阶段3方案ABACAD收角对照_v1002.sh`

执行备注：

- `2026-05-21` 首次启动 `V/W/X` 后，`V/X` 因 v1002 远端 `BlindNavEnv` 版本不支持 `reward_non_recovery_*` 参数而失败；`W` 正常启动并已保存 `00250` checkpoint。
- 为避免替换远端训练代码带来不可控差异，新增 `Y/Z/AA` 使用远端已支持的 `reward_angle_penalty_scale` 等全局参数，另开批次继续跑。
- 实测 v1002 当前运行代码也不支持全局 `reward_*_scale` 参数，因此 `Y/Z` 同样失败；`AA` 正常启动。
- 后续改为只用已验证可覆盖的 `delta_angle_limit` 做收角 sweep：`AB=55deg`、`W=45deg`、`AA=50deg`、`AC=40deg`、`AD=35deg`。

默认运行参数：

- `RUN_GROUP_NAME=目标8显式脱困v10阶段3方案VWX角度对照_v1002`
- `STAGE3_EPISODES=500`
- `CHECKPOINT_INTERVAL=250`
- `N_ENVS=96`
- `EVAL_EPISODES=20`
- `VIDEO_EPISODES=0`
- `RESUME_MODEL=runs/目标8显式脱困v10四阶段_v1002_110k/.../stage2/checkpoint_ep_03000.zip`

判定重点：

- 首要：普通成功率、普通平均步数、普通平均角度。
- 必须保留：压力成功率、pressure recovery escape rate、压力碰撞数。
- 如果角度下降但 recovery rate 归零、碰撞升高，不能替代 v10A。

手动复验结论（2026-05-21）：

- 复验目录：`runs/目标8显式脱困v10阶段3收角手动显式评估_v1002/`
- 复验对象：`AB=55deg`、`AA=50deg`、`W=45deg`、`AC=40deg`、`AD=35deg` 的 `checkpoint_ep_00500`
- 复验命令显式传入 `--env-overrides-file`，并额外用探针确认 `make_state_env()` 能读到 55/50/45/40/35 度。
- 普通/压力 20 局结果五个方案完全相同：普通成功率 `1.00`、平均步数 `36.45`、平均角度 `34.67deg`、recovery rate `0.245`；压力成功率 `0.95`、平均步数 `46.65`、平均角度 `40.55deg`、recovery rate `0.327`、escape rate `0.90`、平均碰撞 `9.85`。
- 原因不是评估没读覆盖值，而是策略在抽查种子里方向动作全为中间档 `direction_bin=2`，即目标方向 0 偏移；因此 `delta_angle_limit` 上限没有被实际使用。
- 结论：`delta_angle_limit` 收角 sweep 对当前 v10A 系列是 no-op，不能再作为角度优化主线。v10A 的角度误差主要来自 heading 滞后、碰撞/脱困宏动作和盲状态动力学，而不是离散角度档太粗。

下一步：

- 在 v1002 上建立独立新版代码副本，使用本地已支持的 `reward_*_scale` 和 `reward_non_recovery_*_scale` 开有效的奖励压角短训。
- 不覆盖 v1002 原主目录，输出单独 run group，保留旧模型和回退路径。

新版代码副本与有效压角实验（2026-05-21）：

- 独立代码副本：`/home/diana/fishing/blind_nav_rl_reward_code_20260521`
- 结果仍写入主目录 runs，便于统一拉取：`/home/diana/fishing/blind_nav_rl/runs/`
- 失败批次：`目标8显式脱困v10阶段3新版压角_v1002`
  - 原因：首次启动时忘记显式设置 `ROOT=/home/diana/fishing/blind_nav_rl_reward_code_20260521`，实际仍用旧主目录代码，`reward_*_scale` 参数不兼容。
  - 处理：停止该批次空 watcher；保留失败日志作为回退记录，不删除。
- 有效批次1：`目标8显式脱困v10阶段3新版压角_codecopy_v1002`
  - `V`：非 recovery 轻压角；`500/1000` 基本等于 v10A，`1500` 坍缩为 recovery rate `0.0`、压力碰撞 `31.3`。
  - `Q`：全局轻压角；`1000` 压力成功率 `1.00`、压力角度 `36.26deg`，但普通成功率 `0.95`、普通角度 `37.41deg`，不能替代 v10A；`1500` 坍缩。
  - `R`：保脱困压角；`500` 压力角度 `38.65deg`，但普通成功率 `0.95`、步数和碰撞上升；`1000/1500` 坍缩。
- 有效批次2：`目标8显式脱困v10阶段3超轻压角_codecopy_v1002`
  - 新增配置：
    - `configs/v10_stage3/方案AE_A超轻非recovery压角.json`
    - `configs/v10_stage3/方案AF_A超轻全局压角.json`
    - `configs/v10_stage3/方案AG_A保脱困轻压角.json`
  - `AE checkpoint 00500` 当前最好：普通成功率 `1.00`、步数 `36.45`、角度 `34.67deg`；压力成功率 `0.95`、步数 `41.05`、角度 `38.03deg`、recovery rate `0.291`、escape rate `0.90`、碰撞 `9.0`。
  - 对比 v10A：普通持平；压力步数 `46.65 -> 41.05`，压力角度 `40.55 -> 38.03`，碰撞 `9.85 -> 9.0`；recovery rate 从 `0.327` 降到 `0.291`，但 escape rate 保持 `0.90`。
  - `AE/AF checkpoint 01000` 坍缩为 recovery rate `0.0`，不能用。
  - `AG 500/1000` 与 v10A 基本相同，说明 recovery bonus 过强时压角改动没有进入策略。

当前候选排序：

| 候选 | 状态 | 结论 |
|---|---|---|
| `AE checkpoint 00500` | 新最佳候选 | 普通不退，压力步数/角度/碰撞小幅优于 v10A，仍保留 recovery |
| `v10A` | 稳定回退 | 原始最佳，指标稳定，视频已拉取 |
| `Q checkpoint 01000` | 压力专项候选 | 压力指标好，但普通退化，不能作为通用最佳 |
| `AE/AF/Q/R 后期 checkpoint` | 淘汰 | recovery rate 归零，碰撞上升，复现旧压角坍缩问题 |

AE500 复验补充（2026-05-21）：

- 多 seed 训练复验批次：`目标8显式脱困v10阶段3_AE500多seed复验_codecopy_v1002`
- 配置：5 个独立 seed，均使用 `方案AE_A超轻非recovery压角.json`，只训练到 `500`，避免 `1000` 后坍缩。
- 结果：
  - 5 个 seed 中 4 个与 v10A 指标完全一致。
  - 1 个 seed 走向“低角度但 recovery/碰撞退化”：普通成功率 `0.95`、普通角度 `20.88deg`；压力成功率 `0.90`、压力角度 `13.14deg`、recovery rate `0.016`、escape rate `0.25`、碰撞 `30.65`。
- 结论：AE 配置本身不是稳定地产出更好模型；原始 `AE checkpoint 00500` 是可用候选，但不能说明这个训练 recipe 足够稳。

大样本复评：

- 复评目录：`目标8显式脱困v10_A_vs_AE500_大样本复评_v1002`
- 复评模型：
  - v10A：`方案A保留线_v1002/.../checkpoint_ep_01500.zip`
  - AE500：`超轻压角_codecopy_v1002/.../checkpoint_ep_00500.zip`
- 评估口径：普通 `100` 局，压力 `100` 局，均不生成视频。

| 指标 | v10A | AE500 |
|---|---:|---:|
| 普通成功率 | `1.00` | `1.00` |
| 普通平均步数 | `44.60` | `45.01` |
| 普通平均角度 | `34.88deg` | `34.74deg` |
| 普通 recovery rate | `0.255` | `0.237` |
| 普通平均碰撞 | `6.42` | `6.52` |
| 压力成功率 | `0.91` | `0.92` |
| 压力平均步数 | `59.42` | `54.60` |
| 压力平均角度 | `44.23deg` | `43.53deg` |
| 压力 recovery rate | `0.367` | `0.336` |
| 压力 escape rate | `0.96` | `0.96` |
| 压力平均碰撞 | `11.71` | `10.86` |

当前判断：

- 如果只选“当前已有 checkpoint”，`AE500` 略优于 v10A：普通基本持平，压力成功率/步数/角度/碰撞略好。
- 如果看“训练方案可复现性”，v10A 更稳；AE 需要更保守的变体或早停选择器。
- 因此暂定当前最佳候选为 `AE500`，稳定回退仍是 `v10A`。

保守 AE 派生批次（2026-05-21）：

- 批次：`目标8显式脱困v10阶段3保守AE派生_codecopy_v1002`
- 配置：
  - `AH`：`方案AH_A极轻非recovery压角.json`
  - `AI`：`方案AI_A超轻压角强保脱困.json`
  - `AJ`：`方案AJ_A超轻压角晚脱困gate.json`
- 训练：各 `500` episode，`96` env，仍从 v10 stage2 checkpoint 03000 续。

结果：

| 方案 | 普通成功 | 普通步数 | 普通角度 | 压力成功 | 压力步数 | 压力角度 | 压力 recovery | 压力碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| AH | `1.00` | `36.45` | `34.67` | `0.95` | `46.65` | `40.55` | `0.327` | `9.85` | 基本等于 v10A |
| AI | `1.00` | `36.45` | `34.67` | `0.95` | `46.65` | `40.55` | `0.327` | `9.85` | 基本等于 v10A |
| AJ | `1.00` | `36.05` | `33.99` | `0.95` | `46.65` | `40.55` | `0.303` | `9.85` | 普通略好，压力不变，不如 AE500 |

结论：

- 更保守的 AE 派生没有超过原始 `AE500`。
- 当前“已验证 checkpoint 最优”仍是 `AE500`。
- 当前“最稳可复现回退”仍是 `v10A`。

v10b9 与终点收角续训补充（2026-05-21）：

- `v10b9` 分支已完成短训与 watcher 评估，方向 action 从 5 档改为 9 档。
- `stage3 01000` 已评估，不缺结果；但 pressure success 只有 `0.90`，pressure recovery rate 为 `0.000`，碰撞 `31.30`。
- `v10b9` 最好的可比 checkpoint 仍是 `stage2a 00500`，20 局 pressure success `0.95`、终点角度 `3.64deg`，但 50 局复评变为 pressure success `0.94`、终点角度 `5.87deg`、last5 `5.34deg`、碰撞 `10.44`，不超过 `AE500`。

基于 `AE500` 的继续短训：

- 批次：`目标8显式脱困v10阶段3终点收角保脱困_v1002`
- 配置：`方案AI_A超轻压角强保脱困.json`
- 起点：`AE500 checkpoint_ep_00500.zip`

| checkpoint | 普通成功 | 普通终点角 | 普通 recovery | 压力成功 | 压力终点角 | 压力 last5 | 压力 recovery | 压力 escape | 压力碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `00250` | `1.00` | `0.00007deg` | `0.215` | `0.95` | `7.60deg` | `3.06deg` | `0.279` | `0.90` | `9.00` | 成功率略高，但终点角不如 `AE500` |
| `00500` | `0.90` | `0.050deg` | `0.044` | `0.90` | `0.00004deg` | `0.00002deg` | `0.006` | `0.10` | `31.00` | recovery 坍缩，淘汰 |

新增终点角度专用奖励分支：

- 代码：`BlindNavEnv` 新增 `reward_terminal_align_bonus_scale`，只放大 `reached` 时的终点对齐奖励，不再增加全轨迹角度惩罚。
- 配置：`configs/v10_stage3/方案AK_A终点收角保脱困.json`
- 启动脚本：`tools/启动_v10阶段3方案AK终点收角_v1002.sh`
- 批次：`目标8显式脱困v10阶段3方案AK终点收角_v1002`

| checkpoint | 普通成功 | 普通终点角 | 普通 recovery | 压力成功 | 压力终点角 | 压力 last5 | 压力 recovery | 压力 escape | 压力碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `00250` | `1.00` | `0.00006deg` | `0.152` | `0.90` | `0.00004deg` | `0.00002deg` | `0.037` | `0.40` | `29.95` | 终点角低但成功/脱困退化，淘汰 |
| `00500` | `0.85` | `0.050deg` | `0.000` | `0.90` | `0.00004deg` | `0.00002deg` | `0.000` | `0.00` | `31.30` | recovery 归零，淘汰 |

当前判断：

- 单纯继续训练 `AE500`，即使只加强“到达时角度”，也会快速滑向 `recovery_rate ~= 0` 的坏解。
- 因此当前不能用“终点角度接近 0”作为单独替换依据，必须同时满足 pressure success 和 recovery/碰撞不退化。
- 当前可交付最优仍是 `AE500`；稳定回退仍是 `v10A`。

AE500 视频与共享产物：

- 本地共享目录：`/home/diana/screencap/file/目标8盲导航AE500成果`
- 成果预览页：`http://ai.jiaran.icu:12346/file/目标8盲导航AE500成果/成果预览.html`
- 普通评估拼接视频：`http://ai.jiaran.icu:12346/file/目标8盲导航AE500成果/AE500普通评估3段拼接.mp4`
- 压力评估拼接视频：`http://ai.jiaran.icu:12346/file/目标8盲导航AE500成果/AE500压力评估3段拼接.mp4`
- 复评摘要 CSV：
  - `AE500普通50复评摘要.csv`
  - `AE500压力50复评摘要.csv`

## 当前结论

目前不要把“更多训练轮数”当成唯一解。已经验证过：在没有障碍物输入、没有碰撞标志、没有地图预处理的约束下，模型可以学会大部分朝目标移动，但内凹山体脱困和真实控制延迟仍然是主要风险。

当前比较稳的主线是：

| 用途 | 推荐路线 | 原因 |
|---|---|---|
| 目标 8px 圈内即停 | `observable8_target8_v2_offset60` | 动作围绕目标方向偏移，蛇形比旧的摇杆增量路线少 |
| 轻量部署 | Tiny `64x1` LSTM | 约 4.6 万参数，效果接近大模型 |
| 追求指标 | Large `384x2` LSTM | 100k sweep 里指标最好，但参数和训练耗时明显更高 |
| 内凹山体研究 | `recovery_angle` / `full180_pressure` | 有后退或大角度动作能力，但还没稳定学会显式脱困 |
| 停在目标外 8-20px 并朝向目标 | `observable11_stop_*` | 能表达停止距离和角度误差，但严格任务成功率还不稳定 |
| 新主线候选 | `v9 trigger curriculum` | 去掉策略 jump，统一动作空间做两阶段课程训练 |
| 新主线候选2 | `v10 explicit recovery curriculum` | 保持盲状态，但把 recovery 前信号和 recovery 动作语义拆清楚 |

## 回退索引

| 版本/路线 | 状态 | 动作 | 算法/网络 | 结果 | 回退入口 |
|---|---|---|---|---|---|
| 早期 SAC 盲人寻路 | 10D/旧状态 | 绝对移动角度、速度、jump | SAC MLP | 可跑通，但视频里出现卡住、无意义动作、绕目标等问题 | `train_1000_episodes.py`, `train_sac.py`, `train_10000_episodes_vec64_dense.py` |
| dt=0.3 修复版 | 旧状态 | 原动作 | SAC | 修过“一动不动”问题；问题原因偏向停止/卡住判定和动作更新逻辑，不是山体碰撞 | `train_1000_episodes_vec256_concave_eval1.py` |
| 高密度障碍 SAC | 旧状态 | 原动作 | SAC | 试过更多树/山、山放大、10万到100万轮；盲状态下仍然会重复撞山或绕圈 | `train_100000_episodes_vec256_extreme_eval10.py`, `train_1000000_episodes_vec128_dense_eval20.py` |
| 多算法 10k sweep | 旧观察包装 | 连续 3D | SAC/TD3/DDPG/PPO/A2C/RecurrentPPO | 结论转向 RecurrentPPO + LSTM，因为需要时序记忆 | `benchmark_10k_algos.py` |
| RecurrentPPO 8D delta45 | `observable8_delta45` | 相对当前方向 `[-45,+45]`、速度、jump | LSTM 64x1 | 当时推荐轻量版，约 42k 参数；384 并行反而回归 | `CLAUDE.md`, `benchmark_state_dims_10k.py` |
| 7D stop task | `observable7_stop_*` | 摇杆增量、速度、jump | LSTM 256x2 | 状态很精简，但严格停止任务学习慢，后来改 11D | `train_curriculum_sequence.py` |
| 11D stop curriculum | `observable11_stop_*` | 摇杆增量、速度、jump | LSTM 256x2 | 阶段训练可用；最终严格阶段 6k 轮成功率约 70%，20 次测试 16/20 成功 | `train_observable11_sequence.py`, `artifacts/observable11_final_eval20/` |
| 质量继续训练 | `observable11_stop_quality_delta60` | 摇杆增量 `60deg` | LSTM 256x2 | 目标是提升停止精度和朝向质量；用 quality score 选模型 | `launch_quality_continue.sh` |
| 直线优先 stop | `observable11_stop_line_delta45` | 目标跟踪残差，`angle=0` 表示直线朝目标 | LSTM 256x2 | 用来压蛇形；仍需检查是否在近目标处过晚调整 | `launch_line_continue.sh` |
| target-circle line | `observable8_target8_line_delta90` | 上一摇杆角度增量 `[-90,+90]` | LSTM 256x2 | 自由度大，容易蛇形或绕圈 | `benchmark_state_dims_10k.py` |
| target-circle v2 | `observable8_target8_v2_offset60` | 相对目标方向偏移 `[-60,+60]` | 多个 LSTM 尺寸 | 100k sweep 全部 100% 成功；Tiny 很划算，Large 指标最好 | `launch_target8_v2_parallel.sh`, `eval_target8_sweep.py` |
| recovery angle | `observable8_target8_recovery_angle` | 正常目标偏移 + back/back-left/back-right 选择 | Tiny LSTM 64x1 | 100k 后正常成功率 1.00，压力成功率 0.85，但显式 recovery 使用率 0 | `launch_target8_recovery_angle_tiny.sh` |
| recovery pressure | `observable8_target8_recovery_pressure` | 同上 | Tiny LSTM 64x1 | 32k checkpoint 压力成功率 0.75，仍不使用 recovery | `benchmark_state_dims_10k.py` |
| full180 pressure | `observable8_target8_full180_pressure` | 允许更大角度范围 | Tiny LSTM 64x1 | 50k 后正常成功率 0.95，压力成功率 0.90；仍没稳定学会后退策略 | `benchmark_state_dims_10k.py` |
| macro recovery 半山高密度 | `observable8_target8_macro_recovery_dense_halfmountain` | v2 目标方向偏移 + 宏 recovery，后退/侧退保持 4 步 | Tiny/Current LSTM | 2026-05-19 已启动 10k smoke；山体半径减半、山体数量翻倍 | `launch_target8_macro_recovery_dense_halfmountain.sh` |
| trigger macro recovery 半山高密度 | `observable8_target8_macro_trigger_dense_halfmountain` | 默认 v2 目标方向偏移；第 4 维高于阈值才触发 4 步宏 recovery | Tiny LSTM 64x1 | 10k 后普通 95%，压力 75%，但 recovery 使用率仍 0 | `launch_target8_macro_trigger_dense_halfmountain.sh` |
| v4 12D recovery 预留 | `observable12_target8_recovery_v4` | 8D target state + 4 个 recovery 记忆/节流信号 | Medium LSTM 128x2 | 评估和文档入口已预留；等待环境/wrapper 完整实现和训练结果 | `eval_target8_sweep.py`, `eval_target8_pressure.py` |
| v9 trigger curriculum | `observable12_target8_discrete_trigger_v9_stage1/stage2` | 12D 盲状态；去掉策略 jump；3 维离散触发式 recovery | Medium LSTM 128x2 | 2026-05-19 新增，等待训练验证 | `train_target8_v9_curriculum.py`, `launch_target8_v9_curriculum.sh` |
| v10 explicit recovery curriculum | `observable12_target8_discrete_recovery_v10_stage1/stage2/stage3` | 12D 盲状态；显式 4 类 recovery 模式；三阶段课程 | Medium LSTM 128x2 | 2026-05-19 新增，等待训练验证 | `train_target8_v10_curriculum.py`, `launch_target8_v10_curriculum.sh` |
| 环境速度优化 | 不变 | 不变 | 不变 | AABB + spatial grid 约 15.5x 加速，和旧碰撞结果对齐 | `benchmark_env_speed.py`, `blind_nav_rl/env.py` |

### v9 trigger curriculum 启动记录

新增日期：2026-05-19

目标：

- 去掉策略 jump
- 保留盲状态
- 把“先学直线追目标，再学 trapped-start 脱困”做成统一动作空间下的两阶段课程

实现摘要：

| 项 | 值 |
|---|---|
| stage1 | `observable12_target8_discrete_trigger_v9_stage1` |
| stage2 | `observable12_target8_discrete_trigger_v9_stage2` |
| 动作 | `MultiDiscrete([5, 2, 2])` |
| jump | 不由策略控制 |
| recovery | 触发式 6 步宏动作 |
| 额外分布 | trapped start + opposite heading + 坐标延迟/噪声 |

本地验证：

- `py_compile` 通过
- `conda ML` 下本地 2+2 episode smoke 通过
- 产物目录：
  - `runs/目标8离散触发脱困v9_本地smoke/`
  - `runs/目标8离散触发脱困v9_本地smoke2/`

正式训练启动：

| 主机 | 训练目录 | checkpoint 评估目录 | 最终归档目录 | 启动状态 |
|---|---|---|---|---|
| `253` | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散触发脱困v9课程_253_100k` | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散触发脱困v9_checkpoint评估_253` | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散触发脱困v9_最终归档_253` | 已启动 |
| `v1002` | `/home/diana/fishing/blind_nav_rl/runs/目标8离散触发脱困v9课程_v1002_100k` | `/home/diana/fishing/blind_nav_rl/runs/目标8离散触发脱困v9_checkpoint评估_v1002` | `/home/diana/fishing/blind_nav_rl/runs/目标8离散触发脱困v9_最终归档_v1002` | 已启动 |

启动脚本：

- `tools/启动_v9_253.sh`
- `tools/启动_v9_v1002.sh`
- `watch_v9_checkpoints.py`
- `finalize_v9_audit.py`
- `distributed_v9_progress.py`

早期人工评估：

| 主机 / checkpoint | 普通成功率 | 普通平均步数 | 普通路径效率 | 压力成功率 | 压力平均步数 | 压力路径效率 | 压力 recovery 率 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| `253 stage1 3000` | 0.70 | 96.2 | 0.873 | 1.00 | 58.3 | 0.945 | 0.000 | 早期已能在压力种子上到达，但普通泛化很差，说明更像是在学强约束场景，不是稳定直线能力 |
| `253 stage1 10000` | 1.00 | 23.2 | 0.911 | 0.90 | 54.9 | 约 0.94 | 0.000 | `stage1` 末端普通追目标能力已明显学稳，但压力脱困仍主要靠碰撞滑移，不会主动触发 recovery |
| `253 stage1 14000` | 1.00 | 23.2 | 约 0.91 | 0.90 | 54.9 | 约 0.94 | 0.000 | 与 `stage1 10000` 基本一致，说明 `stage1` 已接近平台，继续加轮数价值不高 |
| `253 stage1 16000` | 1.00 | 23.2 | 约 0.91 | 0.90 | 54.9 | 约 0.94 | 0.000 | 与 `10000/14000` 继续重合，`stage1` 平台判断已足够稳固 |
| `v1002 stage1 10000` | 0.70 | 96.2 | 0.873 | 1.00 | 58.3 | 0.945 | 0.000 | 与 `253 stage1 3000` 基本同型，说明两台机器的早期学习轨迹一致，差异主要是训练推进速度 |
| `253 stage2 1000` | 0.80 | 76.8 | 约 0.89 | 0.90 | 54.9 | 约 0.94 | 0.000 | 刚进入 `stage2` 时普通能力短暂退化，但压力指标与 `stage1` 末端几乎一致，说明新分布暂未学出主动脱困 |

补充观察：

- `253 stage1 8000` 的压力 10 次人工评估里出现 `9/10` 成功，仍然 `recovery_action_rate = 0.000`。
- `253 stage1 10000` 的普通 10 次人工评估已经到 `1.00` 成功率，说明课程第一阶段基本达成了“先学直线追目标”的目标。
- 但 `stage1 10000` 的压力 10 次评估仍然只有 `9/10`，并且 `recovery_action_rate = 0.000`，说明主动脱困这件事还没出现。
- `stage1 14000` 与 `stage1 10000` 的普通/压力结果几乎一致，进一步说明 `stage1` 后半程已经接近平台。
- `stage1 16000` 仍然与 `10000/14000` 基本重合，说明继续盯 `stage1` 已经没有额外信息量。
- `v1002 stage1 10000` 仍然复现“普通偏差、压力能过、recovery 不触发”的早期形态，说明这不是单机偶然现象。
- `253 stage2 1000` 的普通成功率只有 `0.80`，说明切到更难分布后先出现了一次能力回撤；但压力指标与 `stage1` 末端基本没变，`recovery_action_rate` 仍然是 `0.000`。
- 下一观察点应继续看 `stage2 10000` 与 `stage2 30000`，判断这次分布切换能否在后续训练里真正逼出主动 recovery。

### v10 explicit recovery curriculum 启动记录

新增日期：2026-05-19

目标：

- 保持盲状态
- 去掉 `v9` 中“触发前缺少直接卡住信号”的问题
- 把 recovery 动作从“触发 + 方向耦合”改成“显式 recovery 模式”
- 用课程逼出主动后退/侧退脱困

实现摘要：

| 项 | 值 |
|---|---|
| stage1 | `observable12_target8_discrete_recovery_v10_stage1` |
| stage2a | `observable12_target8_discrete_recovery_v10_stage2a` |
| stage2 | `observable12_target8_discrete_recovery_v10_stage2` |
| stage3 | `observable12_target8_discrete_recovery_v10_stage3` |
| 动作 | `MultiDiscrete([5, 2, 4])` |
| jump | 不由策略控制 |
| recovery | 显式 `none / back-left / back / back-right`，环境保持 6 步；`stage2a/2/3` 改为 heading-relative 两段式宏动作 |
| 额外分布 | trapped start + opposite heading + 坐标延迟/噪声 + 强内凹山体 |

当前结论：

- 代码入口已补齐
- `v9` 保留不动，`v10` 作为加法分支并行存在
- 最新修正不再只盯 `pressure_recovery_rate`
- 当前更合理的观察组合是：
  - `pressure_recovery_rate`
  - `pressure_avg_recovery_steps_to_trigger`
  - `pressure_recovery_escape_rate`

远端验证：

- 本地 `conda ML` smoke 通过：`runs/目标8显式脱困v10_本地smoke/`
- 本地压力评估 smoke 通过：`runs/目标8显式脱困v10_本地压力smoke/`
- `253` 远端 smoke 通过：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10_远端smoke/`

正式长训启动记录：

| 设备 | 训练目录 | checkpoint 评估目录 | 状态 | 说明 |
|---|---|---|---|---|
| `253` | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10课程_253_100k` | 未启动 | 已停 | 启动后 `load>10`，不符合当前 AI 资源规范，已停止 |
| `v1002` | `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10课程_v1002_100k` | `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10_checkpoint评估_v1002` | 已启动 | `v1002` 资源检查通过，训练和 watcher 均已挂起 |

当前 `v1002` 进程：

- 旧版训练 pid：`1841701`，已停止
- 旧版 watcher pid：`1852204` / `1865921`，已停止
- 新版训练 pid：`2402797`
- 新版 watcher pid：`2402799`

当前监控命令：

```bash
rtk ssh v1002 'cd /home/diana/fishing/blind_nav_rl && tail -f runs/目标8显式脱困v10课程_v1002_100k/训练.log'
rtk ssh v1002 'cd /home/diana/fishing/blind_nav_rl && tail -f runs/目标8显式脱困v10_checkpoint评估_v1002/watcher.log'
rtk python3 distributed_v10_progress.py --runs v1002:/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10课程_v1002_100k
```

2026-05-19 运行中修正：

首个 `v1002 stage1 1000` checkpoint 暴露了一个明确问题：

| checkpoint | 普通成功率 | 压力成功率 | 压力 recovery 率 | 结论 |
|---|---:|---:|---:|---|
| 旧版 `stage1 1000` | 0.00 | 0.00 | 1.00 | `stage1` 奖励和动作语义把策略推成了“全程 recovery” |

针对这个问题做了两处修正：

1. `stage1` wrapper 里直接禁用 recovery 生效，只保留动作维度兼容，保证第一阶段必须先学正常追目标
2. `stage1` reward 改成 `target_line_macro_recovery_v10_stage1`，进一步压制无意义 recovery

本地验证结果：

| 版本 | 轮数 | 普通成功率 | 压力成功率 | 压力 recovery 率 | 结论 |
|---|---:|---:|---:|---:|---|
| 修正后 `stage1` | 1000 | 0.90 | 1.00 | 0.00 | 已回到“先学直线追目标”的预期轨道 |

因此：

- 旧版 `v1002` 长训已停止并归档到带时间戳的旧目录
- 修正后的 `v10` 已重新在 `v1002` 启动
- watcher 继续先盯 `stage1` 的每 `1000` checkpoint，确认直线能力稳定后再继续盯后续阶段

修正后远端 `v1002 stage1 1000` 已验证：

| checkpoint | 普通成功率 | 普通平均步数 | 普通路径效率 | 压力成功率 | 压力平均步数 | 压力路径效率 | 压力 recovery 率 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 新版 `stage1 1000` | 1.00 | 21.4 | 0.925 | 1.00 | 23.35 | 0.944 | 0.00 | 远端结果与本地修正一致，说明第一阶段已稳定学会直线追目标，没有再次塌成“全程 recovery” |

当前下一观察点：

- 继续让修正后的 run 推进到 `stage2`
- 首个关键检查点改为 `stage2 1000`
- 核心观察指标：`pressure_recovery_rate` 是否开始大于 `0.0`

2026-05-20 `253` 阶段 3 压力微调与复验补记：

背景：

- `253` 的 Python / numpy / sb3 版本比 `v1002` 旧，不能直接加载 `v1002` 产出的 `RecurrentPPO zip checkpoint`
- 已改成先在 `v1002` 导出 `policy.state_dict()`，再由 `253` 用 `--init-policy-state-dict` 初始化 `stage3`
- 这样避免了 `ModuleNotFoundError: numpy._core` 的跨环境恢复问题

这一轮在 `253` 上只重跑 `v10 stage3`，不重做前两阶段，主要看“主动脱困”能否在压力场景里稳定出现。

#### `253 stage3` 对照结果

| 路线 | checkpoint | 普通成功率 | 普通平均步数 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力 recovery 率 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 方案A 保留线 | `250` | 较强 | 较低 | `0.0` | 可用 | 可用 | `0.0` | 普通导航稳，但完全没学出主动脱困 |
| 方案D 高压脱困 | `250` | 明显退化 | 偏高 | `>0` | 可触发 | 偏高 | `>0` | 第一次逼出主动脱困，但把正常导航拉坏了 |
| 方案E 平衡高压 | `250` | `1.00` | `58.1` | `0.309` | `0.80` | `86.5` | `0.342` | 首次出现“普通导航还能接受，且压力下有主动脱困” |
| 方案F 保守平衡 | `250` / `500` | `0.80` | 偏高 | `0.0` | `0.95` | 可用 | `0.0` | 压回到“不主动脱困” |
| 方案G 轻压平衡 | `250` | `1.00` | `58.5` | `0.315` | `0.85` | `76.75` | `>0` | 目前最好的单次结果，普通和压力都最平衡 |
| 方案H 激进平衡 | `250` | 可用 | 偏高 | `>0` | 低于 `G` | 偏高 | `>0` | 比 `G` 更激进，但总体不如 `G` |

补充判断：

- `E/G/H` 这组都说明了一个事实：`stage3` 的有效行为窗口出现在早期 checkpoint，继续拉长训练经常会塌回 `recovery_rate = 0`
- 目前最像“可保留主线”的是 `方案G @ 250`
- 但它还不能直接定为最终方案，因为需要复验 seed 稳定性

#### `方案G @ 250` 单独复验

复验目录：

#### `方案A` 多 seed 复验结果（`253`）

复验目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案A多次复验_253`
- 评估目录：
  - `/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案A多次复验_253/01_方案A收紧gate_seed16155000_ep1500_checkpoint评估`
  - `/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案A多次复验_253/02_方案A收紧gate_seed16355000_ep1500_checkpoint评估`
  - `/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案A多次复验_253/03_方案A收紧gate_seed16555000_ep1500_checkpoint评估`

执行约束：

- 设备：`253`
- 资源限制：`CPU < 70%`、`内存 < 70%`
- 训练并发执行
- checkpoint 评估改为串行补跑，避免多个 watcher 同时跑时把 CPU 顶高

3 个 seed 最终结果（`01500` 与 `00500/01000` 一致）：

| 路线 | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 压力 escape 率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `A seed16155000` | `0.85` | `60.75` | `11.04 deg` | `0.000` | `0.90` | `47.20` | `10.85 deg` | `0.000` | `0.000` | `31.20` | 退回“低角误差直走”，完全不触发主动脱困 |
| `A seed16355000` | `1.00` | `36.45` | `34.67 deg` | `0.245` | `0.95` | `46.65` | `40.55 deg` | `0.327` | `0.90` | `9.80` | 复现出主动脱困，但角误差明显变大 |
| `A seed16555000` | `0.85` | `60.75` | `11.04 deg` | `0.000` | `0.90` | `47.20` | `10.85 deg` | `0.000` | `0.000` | `31.20` | 与 `seed16155000` 同型，再次塌回“无主动脱困” |

均值（3 seed）：

| 指标 | 均值 |
|---|---:|
| 普通成功率 | `0.900` |
| 普通平均步数 | `52.65` |
| 普通平均角误差 | `18.92 deg` |
| 普通 recovery 率 | `0.082` |
| 压力成功率 | `0.917` |
| 压力平均步数 | `47.02` |
| 压力平均角误差 | `20.75 deg` |
| 压力 recovery 率 | `0.109` |
| 压力 escape 率 | `0.300` |
| 压力平均碰撞 | `24.07` |

与当前 `K` 保留线对比：

| 路线 | 普通成功率均值 | 压力成功率均值 | 压力 recovery 率均值 | 压力 escape 率均值 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| `K` 多 seed | `0.95` | `0.883` | `0.304` | `0.90` | `12.02` | 当前最稳的稳定优先保留线 |
| `A` 多 seed（253） | `0.900` | `0.917` | `0.109` | `0.300` | `24.07` | 压力成功率不差，但主动脱困和 escape 明显不稳，且碰撞更高 |

这一轮的结论是：

- `A` 在 `253` 的多 seed 复验里存在明显分叉
- 它没有稳定挑战掉 `K`
- 因此当前默认保留线仍然是 `K`
- `A` 可以保留为“分布侧收紧 gate 的有效候选”，但不能直接当作 `253 stage3` 的默认收口

补充说明：

- 这里的结论只针对 `253` 上这条 `stage2 policy.state_dict -> stage3` 的初始化链路
- 它不直接否定前面 `v1002` 上 `方案A保留线` 的单线复现结果
- 更准确的说法是：
  - `v1002` 单线 `A` 可以复现
  - 但 `253` 多 seed 复验说明，`A` 还没有达到可以替代 `K` 的稳定性

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案G复验_253`
- 评估目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案G复验_checkpoint评估_253`

复验结果：

| checkpoint | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `250` | `0.80` | `73.55` | `10.59 deg` | `0.0` | `0.90` | `50.15` | `7.72 deg` | `0.0` | 没有复现出原先 `G@250` 的主动脱困，说明 seed 敏感 |

当前收口：

- `方案G @ 250` 仍是目前最好的单次候选，但稳定性不足，不能直接升格为默认主线
- 下一步不该再做大结构改动，而是要做多 seed 短窗复验，先量化“主动脱困出现率”
- `253` 上旧 watcher 已清掉，避免继续占句柄和干扰 load 观察

#### `方案G` 多 seed 短窗复验

结论补充：

- `方案G` 单次最好看，但复验后没有稳定复现主动脱困
- 因此它更像是“好看的偶然 seed”，不适合直接升格为默认主线

#### `方案K / E` 当前收口判断

2026-05-20 继续在 `253` 上做了一个定向复验，目标不是再扫大量参数，而是验证“高恢复率路线里，`E` 这条是否还能稳定复现”。

复验目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案E复验_253`
- 评估目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案E复验_checkpoint评估_253`

复验结果：

| 路线 | checkpoint | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 压力脱困成功率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `方案E 复验` | `250` | `1.00` | `59.0` | `40.90 deg` | `0.328` | `0.80` | `86.5` | `43.32 deg` | `0.347` | `0.85` | `15.5` | 成功复现“主动脱困存在”，但角误差和路径代价仍然偏高 |

与当前候选汇总对照：

| 候选 | 代表 checkpoint | 普通成功率 | 压力成功率 | 压力 recovery 率 | 压力脱困成功率 | 特征 |
|---|---:|---:|---:|---:|---:|---|
| `方案K` 保留线 | 多 seed 汇总 | 稳定 | `约 0.883` | `约 0.304` | `0.90` | 更像“稳定优先”，恢复率不低，但整体行为偏保守 |
| `方案E` 复验 | `250` | `1.00` | `0.80` | `0.347` | `0.85` | 更像“恢复优先”，能稳定保留主动脱困，但角度/步数成本更高 |
| `方案G` 复验 | `250` | `0.80` | `0.90` | `0.000` | `0.000` | 单次好看，但复验后掉回“无主动脱困” |

当前判断：

- 如果目标是“保住主动脱困行为”，`E` 比 `G` 更可信
- 如果目标是“找一个更稳的保留线”，`K` 仍然是当前最稳的 stage3 候选
- 也就是说，目前其实收敛成两条线：
  - `K`: 稳定优先保留线
  - `E`: 主动脱困优先保留线

下一步建议：

- 不再继续围着 `K` 做小幅 reward 微调
- 后续应优先做两件事之一：
  1. 用统一评分档位正式比较 `K` 和 `E` 的 checkpoint 选择策略
  2. 直接围绕 `E` 做更少量但更有针对性的稳定性复验，而不是重新大扫参

#### `方案E` 多 seed 复验

复验目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案E多次复验_253`

本轮执行约束：

- 设备：`253`
- 资源限制：`CPU < 70%`、`内存 < 70%`
- 训练本体满足限制
- 但 3 个 watcher 并发跑 `eval_target8_sweep.py` 时，会把 `253` 的瞬时 CPU 顶到 `90%+`
- 因此这一轮已改成：训练可以并发，checkpoint 评估必须串行

3 个 seed 结果：

| 路线 | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 压力 escape 率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `E seed15155000` | `1.00` | `59.0` | `40.90 deg` | `0.328` | `0.80` | `86.5` | `43.32 deg` | `0.347` | `0.85` | `15.5` | 复现出主动脱困，但路径和角度代价高 |
| `E seed15355000` | `0.80` | `73.55` | `10.59 deg` | `0.0` | `0.90` | `50.1` | `7.55 deg` | `0.0` | `0.0` | `31.05` | 完全退回“低角误差但无主动脱困” |
| `E seed15555000` | `1.00` | `59.0` | `40.90 deg` | `0.328` | `0.80` | `86.5` | `43.32 deg` | `0.347` | `0.85` | `15.5` | 与第一类 seed 同型，保留主动脱困 |

均值（3 seed）：

| 指标 | 均值 |
|---|---:|
| 普通成功率 | `0.933` |
| 普通平均步数 | `63.85` |
| 普通平均角误差 | `30.79 deg` |
| 普通 recovery 率 | `0.219` |
| 压力成功率 | `0.833` |
| 压力平均步数 | `74.37` |
| 压力平均角误差 | `31.40 deg` |
| 压力 recovery 率 | `0.232` |
| 压力 escape 率 | `0.567` |
| 压力平均碰撞 | `20.68` |

汇总判断：

- `E` 虽然比 `G` 更容易保住主动脱困，但仍然存在明显 seed 分叉
- 也就是说，`E` 不是“稳定主动脱困线”，只能算“较高概率保留主动脱困的候选线”
- 与 `K` 相比，`E` 的优势仍然是 recovery 更积极；劣势则是更大的角误差和更高的路径成本
- 当前最稳的结论仍然没有变：
  - `K`: 更适合当稳定默认线
  - `E`: 更适合当主动脱困探索线

#### `K / E` 收口对比

对比方式：

- 不再新增训练
- 直接对 `253` 上已经完成的 `K` 多 seed 与 `E` 多 seed 结果做统一口径比较
- 资源约束继续保持：`CPU < 70%`、`内存 < 70%`

对比结论：

| 路线 | 普通成功率均值 | 压力成功率均值 | 压力 recovery 率均值 | 压力 escape 率均值 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| `K` 多 seed | `0.95` | `0.883` | `0.304` | `0.90` | `12.02` | recovery 稳定保留，pressure escape 最稳，综合更适合默认保留 |
| `E` 多 seed | `0.933` | `0.833` | `0.232` | `0.567` | `20.68` | 有两类 seed 会保留主动脱困，但分叉更大，碰撞和代价明显更高 |

补充观察：

- `K` 的 3 个 seed 全部保留了非零 `pressure_recovery_rate`
- `K` 的 `pressure_recovery_escape_rate` 三个 seed 都维持在 `0.90`
- `E` 则出现了一条明显退化 seed：
  - `pressure_recovery_rate = 0.0`
  - `pressure_recovery_escape_rate = 0.0`
- 这说明 `E` 仍然没有摆脱“会塌回低角误差直走策略”的问题

最终收口：

- 默认保留线：`K`
- 主动脱困探索线：`E`

后续原则：

- 如果目标是选一个当前最稳、最适合继续当基线的 stage3 结果，优先用 `K`
- 如果目标是继续研究“如何把主动脱困保住”，可以继续围绕 `E` 做更小范围的定向探索
- 但在没有更强证据前，不应再把 `E` 当成默认收口方案

#### `253` 串行评估工具补充

背景：

- `253` 上训练本体并发没有问题
- 但多个 watcher 并发跑 `eval_target8_sweep.py / eval_target8_pressure.py` 会把 CPU 顶到 `90%+`
- 这不符合当前资源约束：`CPU < 70%`、`内存 < 70%`

新增工具：

- 脚本：`tools/串行评估_v10批次_253.sh`

用途：

- 对一个批次目录下的多个 run 逐个串行补跑 `watch_v10_checkpoints.py`
- 已经存在 `checkpoint_eval_summary.csv` 的 run 会自动跳过
- 这样后续多 seed 复验可以继续并发训练，但统一改成串行补评估

标准用法：

```bash
rtk ssh 253 'python3 - <<\"PY\"
from pathlib import Path
import subprocess
root = Path("/home/weiaokang/fishing/blind_nav_rl")
script = root / "tools" / "串行评估_v10批次_253.sh"
cmd = [
    "bash", "-lc",
    f"cd {root} && BATCH_ROOT=/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案E多次复验_253 bash '{script}'"
]
print(subprocess.check_output(cmd, text=True))
PY'
```

当前约定：

- `253` 上：
  - 训练可并发
  - checkpoint 评估默认串行
- 以后如再做多 seed 短窗复验，优先复用这个批量串行评估入口，而不是重新起多个 watcher

#### `方案A` 多 seed 复验启动

背景：

- `A: 收紧 gate` 在另一条分布侧实验线上给出过最强单次信号
- 但 `253` 上现有 `A-only` 目录里的 `checkpoint_eval_summary.csv` 存在重复行，不能直接当成稳定性证据
- 因此下一步改成：在 `253` 上补一轮 `方案A` 多 seed 训练，再用串行评估脚本补评估

新增脚本：

- `tools/启动_v10阶段3方案A多次复验_253.sh`

设计约束：

- 默认 `START_WATCHERS=0`
- 也就是：
  - 训练先并发跑
  - watcher 不自动并发启动
  - 训练结束后用 `tools/串行评估_v10批次_253.sh` 统一串行补评估

本轮启动参数：

| 项 | 值 |
|---|---|
| 设备 | `253` |
| 训练目录 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案A多次复验_253` |
| override | `configs/v10_stage3/方案A_收紧gate.json` |
| `RUN_COUNT` | `3` |
| `STAGE3_EPISODES` | `1500` |
| `CHECKPOINT_INTERVAL` | `500` |
| `N_ENVS` | `96` |
| watcher | 延后，训练后串行补评估 |

运行状态（启动时）：

- `cpu_pct = 10.4%`
- `mem_pct = 3.9%`
- 3 个 seed 都已推进到 `progress episodes=250/1500`

当前目的：

- 验证 `A: 收紧 gate` 能否像 `K` 一样，在多 seed 下稳定保留主动脱困和较高 escape 率
- 如果 `A` 的多 seed 稳定性更强，它会成为下一条比 `K` 更值得保留的默认线候选

复验目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案A多次复验_253`

3 个 seed 结果：

| 路线 | 普通成功率 | 普通平均步数 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力 recovery 率 | 压力 escape 率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `G seed10355000` | `1.00` | `58.5` | `0.315` | `0.85` | `76.75` | `0.339` | `0.85` | `13.05` | 复现出较强主动脱困 |
| `G seed10555000` | `0.80` | `73.55` | `0.0` | `0.90` | `50.15` | `0.0` | `0.0` | `30.8` | 退回到不主动脱困 |
| `G seed10755000` | `1.00` | `34.0` | `0.168` | `0.95` | `44.45` | `0.075` | `0.5` | `18.8` | 有主动脱困，但强度明显下降 |

结论：

- `方案G` 不是偶然有效，但稳定性确实不够
- 主动脱困行为会出现，但对 seed 敏感
- 下一步应在 `G` 附近做更窄的分布微调，而不是继续拉长训练

#### `I/J/K` 收角微调对照

对照目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3收角微调对照_253`

结果：

| 路线 | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 压力 escape 率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `方案I 轻压收角` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `75.9` | `41.23 deg` | `0.329` | `0.85` | `12.85` | 三者中最平衡，普通/压力都没有明显退化 |
| `方案J 轻压保脱困` | `0.95` | `60.5` | `41.11 deg` | `0.329` | `0.80` | `86.4` | `43.41 deg` | `0.344` | `0.85` | `15.2` | 更强调主动脱困，但整体退化最明显 |
| `方案K 中压收角` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `76.6` | `44.39 deg` | `0.359` | `0.90` | `13.5` | 脱困更激进，但压力角误差更大 |

当前判断：

- 这组里最适合作为下一条保留线的是 `方案I`
- `方案K` 在 `recovery_escape_rate` 上更强，但角误差更大，不适合作为默认收口
- 下一步应围绕 `方案I` 做多 seed 复验，确认它比 `方案G` 更稳，而不是继续横向扩更多新方案

#### `方案I` 多 seed 短窗复验

复验目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案I多次复验_253`

3 个 seed 结果：

| 路线 | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 压力 escape 率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `I seed11555000` | `0.80` | `73.75` | `13.08 deg` | `0.029` | `0.95` | `39.25` | `9.48 deg` | `0.011` | `0.0` | 约 `24.6` | 基本退回直走，不主动脱困 |
| `I seed11755000` | `0.95` | `59.1` | `38.41 deg` | `0.261` | `0.85` | `76.35` | `41.18 deg` | `0.268` | `0.80` | 约 `13.35` | 复现出中等强度主动脱困 |
| `I seed11955000` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `75.9` | `41.23 deg` | `0.329` | `0.85` | `12.85` | 与单次 `方案I` 基本一致 |

汇总判断：

- `方案I` 比 `方案G` 更容易出现主动脱困，但仍然没有摆脱 seed 敏感
- 这条线存在一个明确分叉：
  - 一类 seed 会保留较高 `recovery_rate`
  - 另一类 seed 会退回“低角误差、低 recovery”的直接导航策略
- 因此 `方案I` 还不能算稳定主线
- 下一步应检查 `方案K` 这种“escape 更强”的分布，是否能在多 seed 下更稳定地保住 recovery

#### `方案K` 多 seed 短窗复验

复验目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案K多次复验_253`

3 个 seed 结果：

| 路线 | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 压力 escape 率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `K seed12155000` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `76.6` | `44.39 deg` | `0.359` | `0.90` | `13.5` | 与单次 `方案K` 基本一致 |
| `K seed12355000` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `76.6` | `44.39 deg` | `0.359` | `0.90` | `13.5` | 与上一条几乎完全重合 |
| `K seed12555000` | `0.95` | `41.55` | `35.24 deg` | `0.215` | `0.95` | `48.65` | `35.02 deg` | `0.194` | `0.90` | 约 `10.85` | recovery 仍保留，但导航质量更好 |

汇总判断：

- `方案K` 的多 seed 稳定性明显好于 `方案I`
- 3 个 seed 都保住了非零 `pressure_recovery_rate`
- 平均指标大致为：
  - `avg_pressure_success ≈ 0.883`
  - `avg_pressure_recovery_rate ≈ 0.304`
  - `avg_pressure_escape_rate = 0.90`
- 相比 `方案I`：
  - recovery 与 escape 更稳
  - 没再出现完全塌成“几乎不 recovery”的 seed
  - 代价是压力角误差仍然偏大

当前收口：

- 在目前已尝试的 `G / I / K` 多 seed 线上，`方案K` 是更合理的阶段性保留线
- 如果后续继续优化，方向不该再是“先保住 recovery”，而应转成“在不破坏 `K` 的 escape 稳定性前提下压低角误差”

#### `K` 周边收角微调对照

对照目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案K收角微调对照_253`

结果：

| 路线 | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 压力 escape 率 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `方案L K轻收角` | `0.95` | `58.2` | `38.40 deg` | `0.272` | `0.85` | `71.65` | `42.71 deg` | `0.312` | `0.90` | 在不明显破坏 recovery 的前提下，步数和角误差都优于 `K` |
| `方案M K保脱困收角` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `76.15` | `43.85 deg` | `0.355` | `0.90` | 更接近原始 `K`，但收角收益不明显 |
| `方案N K轻压收角` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `75.75` | `43.19 deg` | `0.349` | `0.90` | 比 `M` 略好，但仍不如 `L` 平衡 |

当前判断：

- `方案L` 是 `K` 周边这轮最值得继续复验的线
- 下一步不再扩更多相邻配置，而是直接做 `方案L` 多 seed 短窗复验

#### `方案L` 多 seed 短窗复验

复验目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案L多次复验_253`

3 个 seed 结果：

| 路线 | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 压力 escape 率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `L seed13355000` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `76.15` | `43.85 deg` | `0.355` | `0.90` | 约 `13.6` | 与 `方案M` 基本重合，没有体现出预期的收角收益 |
| `L seed13555000` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `76.15` | `43.85 deg` | `0.355` | `0.90` | 约 `13.6` | 与上一条完全重合 |
| `L seed13755000` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `76.15` | `43.85 deg` | `0.355` | `0.90` | 约 `13.6` | 与上一条完全重合 |

汇总判断：

- `方案L` 的多 seed 结果没有复现出单次对照里看到的收角优势
- 从稳定性上看它不差，但也没有优于当前 `方案K` 保留线
- 因此当前不把 `方案L` 升级为新的保留线

当前收口：

- 目前 `方案K` 仍然是更合理的阶段性保留线
- `K` 周边这一轮微调没有带来可重复的角误差改善
- 如果后续继续，方向不该再在这组超近邻参数上横向扫，而应该换到更明确的角度约束来源或奖励侧调整

#### `K` 奖励侧微调对照

对照目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案K奖励微调对照_253`

结果：

| 路线 | 普通成功率 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均角误差 | 压力 recovery 率 | 压力 escape 率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `方案P K奖励压角` | `0.80` | `10.53 deg` | `0.0` | `0.90` | `8.02 deg` | `0.0` | `0.0` | `31.1` | 强压角直接把策略推回“不主动脱困”的坏局部最优 |
| `方案Q K奖励轻压角` | `0.80` | `10.53 deg` | `0.0` | `0.90` | `8.02 deg` | `0.0` | `0.0` | `31.1` | 与 `P` 基本重合，说明只要朝这个方向压就会塌 |
| `方案R K奖励保脱困压角` | `0.95` | `40.27 deg` | `0.317` | `0.85` | `44.39 deg` | `0.359` | `0.90` | `13.5` | 基本退回原始 `K`，没有带来新的角度收益 |

汇总判断：

- 奖励侧“直接增强角度惩罚”的这条尝试失败了
- 一旦压得明显，策略就会学成“低角误差但不主动脱困”
- 压得保守时，又只是退回 `K`

当前收口：

- 现阶段 `方案K` 仍然是最合理的阶段性保留线
- 下一步不应继续在这一类 reward 系数上横向扫

#### `K` 分段奖励微调对照

对照目录：

- 训练目录：`/home/weiaokang/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案K分段奖励微调对照_253`

结果：

| 路线 | 普通成功率 | 普通平均步数 | 普通平均角误差 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力平均角误差 | 压力 recovery 率 | 压力 escape 率 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `方案S K分段压角` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `76.6` | `44.39 deg` | `0.359` | `0.90` | 与原始 `K` 基本重合，没有体现分段压角收益 |
| `方案T K分段轻压角` | `0.95` | `60.0` | `40.27 deg` | `0.317` | `0.85` | `76.6` | `44.39 deg` | `0.359` | `0.90` | 与 `S` 基本完全重合 |
| `方案U K分段保守压角` | `0.95` | `59.1` | `39.48 deg` | `0.292` | `0.85` | `77.8` | `44.38 deg` | 约 `0.34` | `0.90` | 有极小角度收益，但不显著，且普通 recovery 略降 |

汇总判断：

- “只压正常导航段”的分段奖励思路，没有像全局角度惩罚那样直接把策略打塌
- 但它也没有带来足够稳定、足够明显的角度改善
- `方案U` 有一点点改善，但量级不够，不足以替代当前 `方案K`

当前收口：

- 现阶段 `方案K` 仍然是最合理的阶段性保留线
- 分段奖励也没有带来可观的稳定增益

2026-05-19 运行中加速调整：

由于修正后的 `stage1` 在 `1000/2000/3000` checkpoint 上结果已经稳定重合：

| stage1 checkpoint | 普通成功率 | 压力成功率 | 压力 recovery 率 | 结论 |
|---|---:|---:|---:|---|
| `1000` | 1.00 | 1.00 | 0.00 | 已满足 stage1 目标 |
| `2000` | 1.00 | 1.00 | 0.00 | 与 `1000` 重合 |
| `3000` | 1.00 | 1.00 | 0.00 | 与 `1000/2000` 重合 |

因此不再把 `stage1` 跑满 `20000`，直接用最新稳定 checkpoint 提前切入 `stage2`：

- resume model:
  `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10课程_v1002_100k/rppo_medium_lstm128x2_observable12_target8_discrete_recovery_v10_stage1/models/checkpoint_ep_07000.zip`
- `stage1` 主训练已停止
- `stage2->stage3` 续训已启动
- 续训 pid：`2649884`
- 新 watcher pid：`2649886`

当前状态：

- `distributed_v10_progress.py` 已能识别 `阶段2训练.pid/log`
- 当前 live stage:
  `v10_stage_start stage=2/3 name=rppo_medium_lstm128x2_observable12_target8_discrete_recovery_v10_stage2`
- 当前总进度已按课程折算为：
  `20000/100000`

2026-05-19 新一轮结构修正：

发现门控版 `stage2` 虽然比“全程 recovery 塌缩”明显好，但仍然有两个结构性问题：

1. recovery primitive 仍然是 `target_relative_macro_recovery`
2. `stage1 -> hard stage2` 的难度跳变过大

因此当前分支继续做了三处加法修正：

1. 新增 `stage2a`
   - 作为 `stage1 -> stage2` 的过渡分布
   - 较少山体、较浅内凹、较低 trapped-start
2. `stage2/3` recovery primitive 改成 `heading_relative_two_phase_macro_recovery`
   - 先按当前 heading 后退
   - 再按当前 heading 左/右侧移
   - 更贴近真实“退出内凹”的几何
3. `stage2a/2/3` 开启 recovery 固定高速
   - 进入 recovery 后不再让策略同时探索 `50/100`
   - 只保留“何时脱困、选哪种脱困模式”两个核心决策

配套修正：

- wrapper 的 selective gate 提高到：
  - `no_progress_time >= 0.8`
  - 或 `recent_collision_norm >= 0.82`
- watcher / finalizer / progress 脚本已同步四阶段
- 压力评估新增：
  - `avg_recovery_steps_to_trigger`
  - `recovery_escape_rate`

这次修改的判断标准也相应变化：

- 不再只看 recovery 是否被触发
- 还要看 recovery 是否触发得更早、触发后是否真的把 stuck/no-progress 压回去

2026-05-20 四阶段 `stage2a` 首批远端结果：

新 run：

- 训练目录：`/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10四阶段_v1002_110k`
- checkpoint 评估目录：`/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10四阶段_checkpoint评估_v1002`
- 启动脚本：`tools/启动_v10四阶段_v1002.sh`

`stage2a` 当前观察点：

| checkpoint | 普通成功率 | 普通平均步数 | 普通路径效率 | 压力成功率 | 压力平均步数 | 压力路径效率 | 压力 recovery 率 | 压力平均触发步数 | 压力脱困成功率 | 压力平均碰撞 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `1000` | 0.95 | 34.4 | 0.765 | 0.95 | 45.65 | 0.672 | 0.427 | 3.5 | 0.90 | 9.2 |
| `2000` | 0.95 | 34.4 | 0.765 | 0.95 | 45.65 | 0.672 | 0.427 | 3.5 | 0.90 | 9.2 |
| `3000` | 1.00 | 22.45 | 0.815 | 0.95 | 47.4 | 0.632 | 0.441 | 3.5 | 0.90 | 10.55 |

当前解释：

- `stage2a` 已经证明四阶段改法没有退回旧的两种坏形态：
  1. `recovery_rate = 0`
  2. 全程塌成 `recovery`
- recovery 在压力图里会被较早触发，平均第 `3.5` 步就进入
- recovery 触发后，约 `90%` 的 episode 能把 stuck/no-progress 压回低位
- 普通追目标能力在 `stage2a 3000` 已经恢复到 `1.00`

因此下一真正关键观察点变成：

- `stage2 checkpoint 1000`
- `stage2 checkpoint 3000`

重点不再是“会不会用 recovery”，而是“进入 hard trap 后这些 recovery 是否还保持有效”。

2026-05-20 `stage2 checkpoint 1000`：

| checkpoint | 普通成功率 | 普通平均步数 | 普通路径效率 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力路径效率 | 压力 recovery 率 | 压力平均触发步数 | 压力脱困成功率 | 压力平均碰撞 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `stage2 1000` | 0.95 | 35.05 | 0.716 | 0.205 | 0.95 | 47.4 | 0.632 | 0.441 | 3.5 | 0.90 | 10.55 |

和 `stage2a 3000+` 对比：

- 压力指标基本持平，没有在进入 hard trap 后立刻塌掉
- 普通指标有轻微回撤：
  - 平均步数从 `22.45` 升到 `35.05`
  - 路径效率从 `0.815` 降到 `0.716`
  - 平均角度误差也变大

当前判断：

- 四阶段结构修正是成立的
- `stage2` 至少到 `1000` 为止，没有重现旧门控版那种“切 hard stage 后立即崩掉”的问题
- 下一步继续观察 `stage2 3000`
- 在 `stage2 3000` 之前，不急着做新的结构改动

2026-05-20 `stage2 3000 / 4000 / 5000` 观察：

| checkpoint | 普通成功率 | 普通平均步数 | 普通路径效率 | 压力成功率 | 压力 recovery 率 | 压力脱困成功率 | 压力平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| `stage2 3000` | 0.95 | 35.05 | 0.716 | 0.95 | 0.441 | 0.90 | 10.55 | 与 `stage2 1000` 基本持平，hard trap 能力暂时稳住 |
| `stage2 4000` | 0.95 | 38.65 | 0.779 | 0.90 | 0.176 | 0.75 | 11.9 | 开始出现退化，pressure 恢复能力下降 |
| `stage2 5000` | 0.90 | 42.2 | 0.866 | 0.90 | 0.000 | 0.00 | 31.3 | 明显过训，重新退回“几乎不用 recovery，靠贴障滑移和高碰撞” |

当前结论更新：

- `stage2` 的最优区域在 `1000` 到 `3000` 之间
- 继续无脑跑 `stage2` 会把已经学出的 recovery 行为洗掉
- 这说明当前问题已经从“学不会 recovery”转成了“如何保住 recovery，不在后续训练里退化”

因此新的执行策略是：

1. 不再把 `stage2` 当成必须跑满 `30000` 的阶段
2. `stage2` 应该按 checkpoint 选择最优点，当前候选优先 `1000` 或 `3000`
3. 下一步更合理的是：
   - 从 `stage2` 的最佳 checkpoint 直接切入 `stage3`
   - 而不是继续把 `stage2` 往后拉

2026-05-20 当前执行决策：

- 观察到 `stage2 4000` 开始退化，`stage2 5000` 已明显过训
- 因此不再继续让 `stage2` 往后跑
- 当前实际切换为：
  - resume model:
    `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10四阶段_v1002_110k/rppo_medium_lstm128x2_observable12_target8_discrete_recovery_v10_stage2/models/checkpoint_ep_03000.zip`
  - `stage3` 已启动
  - live stage:
    `v10_stage_start stage=4/4 name=rppo_medium_lstm128x2_observable12_target8_discrete_recovery_v10_stage3`

2026-05-20 `stage3` 首轮观察后的二次修正：

观察到：

| checkpoint | 普通成功率 | 普通平均步数 | 普通路径效率 | 普通 recovery 率 | 压力成功率 | 压力 recovery 率 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---|
| `stage2 3000` | 0.95 | 35.05 | 0.716 | 0.205 | 0.95 | 0.441 | 当前最佳平衡点 |
| `stage3 1000` | 0.95 | 74.3 | 0.492 | 0.484 | 0.95 | 0.441 | 压力能力没变，但普通场景被推成过度 recovery |

这说明：

- `stage3` 不是“继续学会更强脱困”
- 而是把原本只该在 trapped case 使用的 recovery，扩散到了普通追目标 episode
- 问题不在“训练还不够”，而在 `stage3` 分布和执行约束过重

因此做了新一轮结构修正：

1. `stage3` 降难
   - `mountain_count: 38 -> 30`
   - `deep_concave_mountain_probability: 0.88 -> 0.60`
   - `trapped_start_probability: 0.75 -> 0.50`
   - `opposite_heading_probability: 0.55 -> 0.42`
2. selective recovery gate 收紧
   - `no_progress_time >= 1.10`
   - 或 `recent_collision_norm >= 0.90`
3. `heading_relative_two_phase_macro_recovery` 增加提前退出
   - 仅在 recovery 确实是从 stuck 状态启动时才允许提前退出
   - 一旦碰撞间隔恢复、`no_progress_time` 回落，或者 recovery 已带来明确正进展，就提前结束剩余宏动作
4. `v10` reward 新增两条约束
   - 非 stuck / 非近期碰撞时继续使用 recovery，额外扣分
   - recovery 已经把 episode 拉回正常状态后，如果还继续保持 recovery，再额外扣分

本地验证：

- `py_compile` 通过
- 四阶段 smoke 通过：
  - `/tmp/目标8显式脱困v10_四阶段_smoke_0520`
- 压力评估 smoke 通过：
  - `/tmp/目标8显式脱困v10_压力smoke_0520`

当前下一步不是继续沿旧 `stage3` 目录往后跑，而是：

- 从 `stage2 checkpoint 03000` 新起一条优化版 `stage3`
- 用新目录和新 watcher 重新验证普通步数能否回落，同时保住压力 recovery 指标

2026-05-20 优化版 `stage3 01000` 首个结果：

新 run：

- 训练目录：`/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3优化版_v1002`
- checkpoint 评估目录：`/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3优化版_checkpoint评估_v1002`
- resume model：
  `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10四阶段_v1002_110k/rppo_medium_lstm128x2_observable12_target8_discrete_recovery_v10_stage2/models/checkpoint_ep_03000.zip`

手动评估 `stage3 01000`：

| 对比项 | 普通成功率 | 普通平均步数 | 普通路径效率 | 普通 recovery 率 | 压力成功率 | 压力平均步数 | 压力路径效率 | 压力 recovery 率 | 压力脱困成功率 | 压力平均碰撞 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `stage2 3000` | 0.95 | 35.05 | 0.716 | 0.205 | 0.95 | 47.4 | 0.632 | 0.441 | 0.90 | 10.55 |
| 旧 `stage3 1000` | 0.95 | 74.3 | 0.492 | 0.484 | 0.95 | 47.4 | 0.632 | 0.441 | 0.90 | 10.55 |
| 新 `stage3 1000` | 1.00 | 46.65 | 0.702 | 0.272 | 0.95 | 48.55 | 0.676 | 0.345 | 0.90 | 10.3 |

当前解释：

- 优化版 `stage3` 已经明显修复“普通场景被推成大面积 recovery”的问题
  - 普通平均步数：`74.3 -> 46.65`
  - 普通路径效率：`0.492 -> 0.702`
  - 普通 recovery 率：`0.484 -> 0.272`
- 压力成功率和脱困成功率保持住了
  - `pressure_success = 0.95`
  - `pressure_recovery_escape_rate = 0.90`
- 但它还没有回到 `stage2 3000` 的普通追目标质量
  - `46.65` 步仍高于 `35.05` 步
  - 普通 recovery 率也仍高于 `0.205`

因此这次修正的结论是：

- 方向正确
- 但还没完全到位
- 下一观察点应继续看优化版 `stage3 2000 / 3000`

2026-05-20 优化版 `stage3 02000` 结果：

| checkpoint | 普通成功率 | 普通平均步数 | 普通路径效率 | 普通 recovery 率 | 压力成功率 | 压力 recovery 率 | 压力平均触发步数 | 压力脱困成功率 | 压力平均碰撞 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 优化版 `stage3 01000` | 1.00 | 46.65 | 0.702 | 0.272 | 0.95 | 0.345 | 3.5 | 0.90 | 10.3 |
| 优化版 `stage3 02000` | 0.90 | 49.9 | 0.784 | 0.052 | 0.90 | 0.011 | 20.6 | 0.20 | 30.65 |

这说明：

- 优化版 `stage3` 的结构修正是有效的，但只在非常早的 checkpoint 有效
- 到 `02000` 时，策略又快速退化成“几乎不用 recovery，靠贴障滑移和高碰撞硬过”
- 和旧 `stage2 4000/5000` 的退化方向一致，只是时间点从更晚提前到了 `stage3 02000`

当前更新后的结论：

1. `stage3` 不适合长训
2. 优化版 `stage3` 的有效窗口目前就在 `01000` 左右
3. 下一步不该继续盲目等 `3000+`
4. 更合理的训练策略是：
   - `stage2` 仍然选 best checkpoint
   - `stage3` 只做短训微调
   - 把 `1000` 或 `1500` 当成候选上限，而不是默认继续拉长

为避免后续再走“长训后再人工挑 checkpoint”的低效路径，已补正式短训选优入口：

- 启动脚本：
  `tools/启动_v10阶段3短训选优_v1002.sh`
- 归档脚本：
  `tools/归档_v10阶段3短训选优_v1002.sh`

默认策略：

- 从 `stage2 checkpoint 03000` 恢复
- `stage3` 只跑 `1500` episodes
- checkpoint 间隔 `500`
- 候选窗口固定为：
  - `0500`
  - `1000`
  - `1500`

配套实现补充：

- `train_target8_v10_curriculum.py` 已支持：
  - `--checkpoint-interval`
  - `--progress-interval`
- `launch_target8_v10_stage2_resume.sh` 已把这两个参数透传给训练脚本
- 本地 smoke 已验证：
  - `--checkpoint-interval 2`
  - `--progress-interval 2`
  能正确产出 `checkpoint_ep_00002.zip` / `checkpoint_ep_00004.zip`

2026-05-20 正式短训选优 run：

- 训练目录：
  `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3短训选优_v1002`
- checkpoint 评估目录：
  `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3短训选优_checkpoint评估_v1002`
- 训练设置：
  - `stage3_episodes = 1500`
  - `checkpoint_interval = 500`
  - `progress_interval = 250`

当前结果：

| checkpoint | 普通成功率 | 普通平均步数 | 普通路径效率 | 普通 recovery 率 | 压力成功率 | 压力 recovery 率 | 压力脱困成功率 | 压力平均碰撞 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `00500` | 1.00 | 46.65 | 0.702 | 0.272 | 0.95 | 0.345 | 0.90 | 10.3 |
| `01000` | 1.00 | 46.65 | 0.702 | 0.272 | 0.95 | 0.345 | 0.90 | 10.3 |
| `02000` | 0.90 | 49.9 | 0.784 | 0.052 | 0.90 | 0.011 | 0.20 | 30.65 |

这里的直接结论是：

- 把 `stage3` 缩成短训线是对的
- 当前最优点没有比旧的 `01000` 更早
- 目前 `00500` 与 `01000` 基本重合
- `02000` 仍然明确过训

因此，当前默认归档候选应优先在：

1. `00500`
2. `01000`

之间选优，而不是继续拉更长

2026-05-20 短训选优最终归档：

- 归档目录：
  `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3短训选优_最终归档_v1002`
- `finalize_v10_audit.py` 在当前评分函数下选择：
  - `final_checkpoint = 00500`
  - score `345.760`

当前解释：

- `01000` 的普通追目标指标更好一些：
  - 普通平均步数 `36.85`
  - 普通 recovery 率 `0.206`
- 但当前评分函数更重视：
  - 压力成功率
  - 压力脱困动作率
  - 压力脱困成功率
  - 压力碰撞数

所以在现有 `checkpoint_score()` 权重下，`00500` 被判为总分更高的 checkpoint。

这也意味着，后续如果更强调普通追目标质量而不是 pressure 脱困强度，就需要单独再调 `finalize_v10_audit.py` 里的评分权重。

2026-05-20 归档评分补充验证：

- `finalize_v10_audit.py` 已支持三种评分档位：
  - `pressure_priority`
  - `balanced`
  - `normal_priority`
- 用当前短训选优 run 的 `00500 / 01000 / 01500` 数据验证后，三种档位都仍然选择 `00500`

这说明：

- 当前 `00500` 的优势不只是“默认评分更偏压力”
- 即使把评分往更均衡、甚至更偏普通追目标方向挪，`00500` 仍然是当前最优点

这一步的含义是：

- `stage2` 不再被当成“训练越久越好”的阶段
- 它现在更像一个“找到最好 hard-trap checkpoint 的筛选阶段”
- 后续真正要验证的是：
  - 用 `stage2 best checkpoint` 进入 `stage3`
  - 能否同时保住普通追目标能力和 hard-trap recovery 能力

2026-05-20 `stage3` 三方案微调对照：

在短训选优确认 `00500` 是当前最优窗口之后，又补了一层更小的 `stage3` 对照，用来区分问题到底更偏向：

1. wrapper gate 太松
2. reset 分布太 aggressive
3. 深凹/陷阱密度过高

新增入口：

- 启动脚本：
  `tools/启动_v10阶段3三方案对照_v1002.sh`
- 通用批量启动：
  `tools/启动_v10阶段3微调对照_v1002.sh`
- 批量归档：
  `tools/归档_v10阶段3三方案对照_v1002.sh`
- 批量汇总：
  `summarize_v10_variant_batch.py`
- A-only 启动：
  `tools/启动_v10阶段3方案A保留线_v1002.sh`
- A-only 归档：
  `tools/归档_v10阶段3方案A保留线_v1002.sh`

三个 override：

- `configs/v10_stage3/方案A_收紧gate.json`
  - `selective_no_progress_threshold = 1.2`
  - `selective_collision_threshold = 0.94`
- `configs/v10_stage3/方案C_降低trapped与反向朝向.json`
  - `trapped_start_probability = 0.38`
  - `opposite_heading_probability = 0.35`
- `configs/v10_stage3/方案B_降低深凹与陷阱密度.json`
  - `mountain_count = 26`
  - `deep_concave_mountain_probability = 0.5`
  - `trapped_start_probability = 0.45`

正式 run：

- 训练批次目录：
  `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3三方案对照_v1002`
- 最终归档目录：
  `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3三方案对照_最终归档_v1002`

统一设置：

- `resume_model = stage2 checkpoint 03000`
- `stage3_episodes = 1500`
- `checkpoint_interval = 500`
- `n_envs = 96`
- 评分档位：`pressure_priority`

最终结果：

| 排名 | 方案 | 选中 checkpoint | 分数 | 普通成功率 | 普通步数 | 压力成功率 | 压力步数 | 压力 recovery 率 | 压力脱困成功率 | 压力碰撞 |
|---:|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | `方案A收紧gate` | `00500` | 347.227 | 1.00 | 36.45 | 0.95 | 46.65 | 0.327 | 0.90 | 9.8 |
| 2 | `方案C降trapped` | `00500` | 344.378 | 1.00 | 45.20 | 0.95 | 51.30 | 0.350 | 0.90 | 9.9 |
| 3 | `方案B降深凹` | `00500` | 334.423 | 1.00 | 38.20 | 0.90 | 62.90 | 0.344 | 0.85 | 10.5 |

结论：

- 当前最优方案是 `A: 收紧 gate`
- `C` 没有翻盘，说明问题不主要是 `trapped_start / opposite_heading` 太激进
- `B` 反而伤到了 pressure 成功率，说明单纯降低深凹和陷阱密度不是当前主矛盾
- 三个方案最终都还是停在 `00500`，再次说明 `stage3` 适合作为短训微调，不适合长训

这一步之后，`stage3` 的默认保留线更新为：

1. `00500 + 方案A收紧gate`
2. `00500 + 方案C降trapped` 作为回退

为避免后续还要从三方案对照脚本里绕一层，已补 `A-only` 正式入口，固定为：

- `resume_model = stage2 checkpoint 03000`
- `env_overrides = configs/v10_stage3/方案A_收紧gate.json`
- `stage3_episodes = 1500`
- `checkpoint_interval = 500`
- `n_envs = 96`

2026-05-20 `方案A保留线` 正式 run：

- 启动脚本：
  `tools/启动_v10阶段3方案A保留线_v1002.sh`
- 归档脚本：
  `tools/归档_v10阶段3方案A保留线_v1002.sh`
- 训练目录：
  `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案A保留线_v1002`
- 最终归档目录：
  `/home/diana/fishing/blind_nav_rl/runs/目标8显式脱困v10阶段3方案A保留线_最终归档_v1002`

结果：

| 方案 | 选中 checkpoint | 分数 | 普通成功率 | 普通步数 | 压力成功率 | 压力步数 | 压力 recovery 率 | 压力脱困成功率 | 压力碰撞 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `方案A收紧gate` | `00500` | 347.227 | 1.00 | 36.45 | 0.95 | 46.65 | 0.327 | 0.90 | 9.8 |

说明：

- 这次单线 run 的 `00500 / 01000 / 01500` 结果与三方案对照中的 `A` 完全一致
- 这证明 `方案A收紧gate` 当前不是批量对照里的偶然最优，而是已经稳定复现的默认保留线

2026-05-19 watcher 运维修正：

在切到 `stage2` 之后，发现 `v10` 的课程 watcher 存在两个运维层问题：

1. 会重复回放 `stage1` 已评估 checkpoint
2. `checkpoint_eval_summary.csv` 会追加重复行，影响后续判断

已做修正：

- `watch_v10_checkpoints.py` 现在会从现有 `checkpoint_eval_summary.csv` 恢复 `seen` 集合
- 新增 `dedupe_v10_checkpoint_summary.py`
- `v1002` 上的 `checkpoint_eval_summary.csv` 已去重，当前保留 `9` 行
- watcher 已重启，当前只会等待新的 `stage2` / `stage3` checkpoint

当前 `stage1` 历史结论已经足够稳定：

- `stage1 1000` 到 `stage1 9000` 的 summary 都保持同一结论：
  - 普通成功率 `1.0`
  - 压力成功率 `1.0`
  - `pressure_recovery_rate = 0.0`

因此当前工作重点已经完全转到：

- `stage2 checkpoint 1000`
- 这将是第一次真正检验主动脱困是否出现的观察点

## 重要指标

### target-circle v2 100k 参数扫描

同一批 20 个测试种子，`distance_to_target <= 8px` 直接终止，模型不负责输出停止速度。

| 模型 | 参数量 | 训练耗时 | 成功率 | 平均步数 | 路径效率 | 平均角度误差 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---|
| Large `384x2` | 3,798,535 | 5160.2s | 1.00 | 16.6 | 0.974 | 10.8deg | 指标最好 |
| Tiny `64x1` | 46,471 | 1106.0s | 1.00 | 16.6 | 0.973 | 11.4deg | 最适合轻量部署 |
| Medium `128x2` | 472,071 | 3020.3s | 1.00 | 16.6 | 0.971 | 11.5deg | 折中 |
| Current `256x2` | 1,696,775 | 4280.7s | 1.00 | 16.4 | 0.972 | 12.1deg | 参数大但不占优 |
| Small `128x1` | 166,407 | 1530.6s | 1.00 | 17.3 | 0.972 | 11.9deg | 不如 Tiny 划算 |

相关产物：

- `http://43.242.75.250:12346/file/blind_nav_target8_v2_final_eval/target8_sweep_summary.csv`
- `http://43.242.75.250:12346/file/blind_nav_target8_v2_final_eval/target8_sweep_summary.md`
- `http://43.242.75.250:12346/file/blind_nav_target8_v2_final_eval/best_rppo_large_lstm384x2_observable8_target8_v2_offset60.zip`
- `http://43.242.75.250:12346/file/blind_nav_target8_v2_final_eval/rppo_large_lstm384x2_observable8_target8_v2_offset60/target8_eval_concat.mp4`

### 11D stop curriculum 结果

本地归档：`artifacts/observable11_final_eval20/observable11_curriculum_results.csv`

| 阶段 | 训练轮数 | 成功率 | 平均步数 | 平均最终距离 | 平均碰撞 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| stage2 | 6000 | 0.95 | 25.6 | 57.74 | 11.65 | 可作为中距离入门 |
| stage3 | 6000 | 1.00 | 17.6 | 37.51 | 0.00 | 阶段任务最好 |
| stage3_heading | 6000 | 0.65 | 98.95 | 33.68 | 0.00 | 朝向约束导致退化 |
| final_warm1 | 6000 | 1.00 | 33.15 | 25.97 | 0.40 | 过渡有效 |
| final_warm2 | 6000 | 1.00 | 36.7 | 20.27 | 0.35 | 过渡有效 |
| strict final | 6000 | 0.70 | 108.2 | 12.78 | 0.80 | 严格停止还不稳定 |

20 次最终测试：`artifacts/observable11_final_eval20/eval_results.csv`

- 成功：16/20
- 失败多发生在接近目标但停止距离或角度不满足时
- 这说明“到达附近”和“精确停圈并朝向目标”是两个难度不同的问题

### recovery 系列结果

| 变体 | 轮数 | 参数量 | 普通成功率 | 压力成功率 | recovery 使用率 | 结论 |
|---|---:|---:|---:|---:|---:|---|
| `recovery_angle` | 100k | 46,537 | 1.00 | 0.85 | 0.000 | 能到达，但没学会显式后退/侧退 |
| `recovery_pressure` | 32k checkpoint | 46,537 | 未完整测 | 0.75 | 0.000 | 压力更强但更差 |
| `full180_pressure` | 50k | 46,471 | 0.95 | 0.90 | 0.000 | 大角度有帮助，但还不是稳定脱困 |

结论：连续动作里把 recovery 做成一个实数选择，探索难度太高。下一步应该把 recovery 做成更容易学的离散动作或短时宏动作。

### macro recovery 半山高密度实验

新增日期：2026-05-19

目标：继承 `target-circle v2` 已经压住蛇形的动作形式，同时给模型一个更容易探索的短时脱困动作。

环境改动：

| 项 | v2 基线 | macro recovery 半山高密度 |
|---|---:|---:|
| 山体数量 | 8 | 16 |
| 山体半径范围 | 70-160 | 35-80 |
| 树数量 | 45 | 45 |
| 树半径 | 18 | 18 |
| action mode | `target_relative_offset` | `target_relative_macro_recovery` |
| recovery 持续 | 无 | 4 step |
| 状态 | 8D | 8D |

新实验：

- `rppo_tiny_lstm64x1_observable8_target8_macro_recovery_dense_halfmountain`
- `rppo_current_lstm256x2_observable8_target8_macro_recovery_dense_halfmountain`

本地/远端验证：

- 本地 `py_compile` 通过：`blind_nav_rl/env.py`, `benchmark_state_dims_10k.py`, `eval_target8_sweep.py`, `eval_target8_pressure.py`, `render_target8_eval.py`
- 本地环境 smoke 通过：8D 状态、4D 动作、16 座山、半径 35-80、macro recovery 生效 4 步
- 远端 253 `py_compile` 通过
- 远端 253 极小训练 smoke 通过：4 episodes、2 envs、1 eval，模型能训练/保存/评估

10k smoke run：

```bash
rtk ssh 253 'cd /home/weiaokang/fishing/blind_nav_rl && tail -f runs/目标8宏脱困山体减半密度翻倍10k/训练.log'
```

进度文件：

- run dir: `/home/weiaokang/fishing/blind_nav_rl/runs/目标8宏脱困山体减半密度翻倍10k`
- pid file: `/home/weiaokang/fishing/blind_nav_rl/runs/目标8宏脱困山体减半密度翻倍10k/训练.pid`
- log file: `/home/weiaokang/fishing/blind_nav_rl/runs/目标8宏脱困山体减半密度翻倍10k/训练.log`

结果：

| 版本 | 普通成功率 | 压力成功率 | 平均角度误差 | recovery 使用率 | 结论 |
|---|---:|---:|---:|---:|---|
| `macro_recovery_dense_halfmountain` Tiny | 0.00 | 0.00 | 压力约 135° | 1.00 | 动作定义有缺陷，模型学成永远斜后退 |
| `macro_trigger_dense_halfmountain` Tiny | 0.95 | 0.75 | 普通 6.3° / 压力 4.35° | 0.00 | 保住直线能力，但 10k 还没学会主动 recovery |
| `macro_trigger_dense_halfmountain` Medium 128x2 | 0.95 | 0.80 | 普通 5.98° / 压力 3.82° | 0.00 | 10k trigger 组里压力成功率最好 |
| `macro_trigger_dense_halfmountain` Current 256x2 | 0.95 | 0.75 | 普通 7.22° / 压力 5.70° | 0.00 | 参数更多没有改善这次 10k 结果 |
| `macro_trigger_dense_halfmountain` Medium 128x2 30k | 1.00 | 0.80 | 普通 6.94° / 压力 4.60° | 0.00 | 普通成功率提高到 100%，但仍没学会主动 recovery |

共享产物：

- 模型：`http://43.242.75.250:12346/file/目标8触发宏脱困山体减半密度翻倍结果/模型.zip`
- 普通视频：`http://43.242.75.250:12346/file/目标8触发宏脱困山体减半密度翻倍结果/普通测试视频.mp4`
- 压力视频：`http://43.242.75.250:12346/file/目标8触发宏脱困山体减半密度翻倍结果/压力测试视频.mp4`
- 训练结果：`http://43.242.75.250:12346/file/目标8触发宏脱困山体减半密度翻倍结果/训练结果.csv`
- 普通指标：`http://43.242.75.250:12346/file/目标8触发宏脱困山体减半密度翻倍结果/普通指标.csv`
- 压力指标：`http://43.242.75.250:12346/file/目标8触发宏脱困山体减半密度翻倍结果/压力指标.csv`

Medium/Current 补跑共享产物：

- 普通详细指标：`http://43.242.75.250:12346/file/目标8触发宏脱困模型对比10k/普通详细指标.csv`
- 中型模型：`http://43.242.75.250:12346/file/目标8触发宏脱困模型对比10k/中型模型.zip`
- 中型普通视频：`http://43.242.75.250:12346/file/目标8触发宏脱困模型对比10k/中型普通详细视频.mp4`
- 中型压力视频：`http://43.242.75.250:12346/file/目标8触发宏脱困模型对比10k/中型压力测试视频.mp4`
- 当前模型：`http://43.242.75.250:12346/file/目标8触发宏脱困模型对比10k/当前模型.zip`
- 当前普通视频：`http://43.242.75.250:12346/file/目标8触发宏脱困模型对比10k/当前普通详细视频.mp4`
- 当前压力视频：`http://43.242.75.250:12346/file/目标8触发宏脱困模型对比10k/当前压力测试视频.mp4`

Medium 30k 扩大训练共享产物：

- 中型 30k 模型：`http://43.242.75.250:12346/file/目标8触发宏脱困中型扩大训练30k/中型模型30k.zip`
- 普通视频：`http://43.242.75.250:12346/file/目标8触发宏脱困中型扩大训练30k/普通测试视频.mp4`
- 压力视频：`http://43.242.75.250:12346/file/目标8触发宏脱困中型扩大训练30k/压力测试视频.mp4`
- 普通详细指标：`http://43.242.75.250:12346/file/目标8触发宏脱困中型扩大训练30k/普通详细指标.csv`
- 压力指标：`http://43.242.75.250:12346/file/目标8触发宏脱困中型扩大训练30k/压力指标.csv`

### v4 12D recovery 结果

新增日期：2026-05-19

训练配置：

| 项 | 值 |
|---|---|
| 实验名 | `rppo_medium_lstm128x2_observable12_target8_recovery_v4` |
| 状态 | `observable12_target8_recovery_v4` |
| 算法 | `RecurrentPPO + MlpLstmPolicy` |
| LSTM | `128x2` |
| MLP | `[128, 128]` |
| 参数量 | `476,297` |
| 模型大小 | `5.51 MB` |
| 训练轮数 | `10000` |
| 训练耗时 | `860.5s` |
| 动作模式 | `target_relative_macro_recovery` |

真实 12 维状态：

| 维度 | 状态 | 说明 |
|---:|---|---|
| 1 | `dx_norm` | 目标相对旧坐标的 x 偏移 |
| 2 | `dy_norm` | 目标相对旧坐标的 y 偏移 |
| 3 | `sin_heading` | 当前朝向正弦 |
| 4 | `cos_heading` | 当前朝向余弦 |
| 5 | `sin_angle_error` | 目标角误差正弦 |
| 6 | `cos_angle_error` | 目标角误差余弦 |
| 7 | `coord_age_norm` | 坐标年龄 |
| 8 | `distance_progress_norm` | 最近距离进展 |
| 9 | `no_progress_time_norm` | 基于观测进展估计的无进展时间 |
| 10 | `distance_norm` | 当前距离归一化 |
| 11 | `last_recovery_mode` | 上一步有效 recovery 模式 |
| 12 | `last_action_angle` | 上一步动作角通道 |

动作解释：

| 维度 | 动作 | 说明 |
|---:|---|---|
| 1 | `angle_offset_norm` | 相对目标方向的偏移 |
| 2 | `speed_norm` | 二值速度 |
| 3 | `jump_norm` | jump/down |
| 4 | `recovery_selector_norm` | normal / back / back-left / back-right |

第 4 维映射：

- `< -0.5` -> normal
- `[-0.5, 0.0)` -> back
- `[0.0, 0.5)` -> back-left
- `>= 0.5` -> back-right

普通 20 次评估：

| 成功率 | 平均步数 | 路径效率 | 平均角度误差 | 平均碰撞 | recovery 使用率 |
|---:|---:|---:|---:|---:|---:|
| 0.95 | 43.75 | 0.974 | 7.57deg | 9.35 | 0.000 |

压力 20 次评估：

| 成功率 | 平均步数 | 平均最终距离 | 路径效率 | 平均角度误差 | 平均碰撞 | recovery 使用率 |
|---:|---:|---:|---:|---:|---:|---:|
| 0.75 | 105.4 | 94.25 | 0.810 | 5.04deg | 66.0 | 0.000 |

结论：

- 12 维版本没有把普通地图直线能力搞坏。
- 普通图角度质量比部分旧版本更好。
- 压力图成功率只有 `0.75`，仍然低于较好的 trigger-macro 基线。
- `recovery_action_rate` 在普通和压力评估里都还是 `0.000`，说明这次新增状态并没有让模型真正学会主动后退/侧退脱困。

产物位置：

- `/home/diana/screencap/file/目标8十二维宏脱困v4结果/模型.zip`
- `/home/diana/screencap/file/目标8十二维宏脱困v4结果/普通测试视频.mp4`
- `/home/diana/screencap/file/目标8十二维宏脱困v4结果/压力测试视频.mp4`
- `/home/diana/screencap/file/目标8十二维宏脱困v4结果/普通汇总指标.csv`
- `/home/diana/screencap/file/目标8十二维宏脱困v4结果/普通详细指标.csv`
- `/home/diana/screencap/file/目标8十二维宏脱困v4结果/压力汇总指标.csv`
- `/home/diana/screencap/file/目标8十二维宏脱困v4结果/压力详细指标.csv`

补充说明：

- 原始 `benchmark_state_dims_10k.py` 训练把模型和 checkpoint 都写出来了，但汇总行在独立评估前被 `BrokenPipeError` 打断，所以 `state_dim_results.csv` 里的聚合指标是空的。
- 上面的结果来自后补的两次独立评估：
  - `runs/目标8十二维宏脱困v4_普通评估20`
  - `runs/目标8十二维宏脱困v4_压力评估20`

### v5 离散宏动作审计版

新增日期：2026-05-19

目标：执行“一步到位审计清单”。这版不覆盖 v4，作为新分支验证“把 recovery 做成离散宏动作后，模型是否能真正学会主动后退/侧退脱困”。

核心改动：

| 项 | v5 设计 |
|---|---|
| 实验名 | `rppo_medium_lstm128x2_observable12_target8_discrete_macro_v5` |
| 状态 | `observable12_target8_discrete_macro_v5` |
| 算法 | `RecurrentPPO + MlpLstmPolicy` |
| LSTM | `128x2` |
| MLP | `[128, 128]` |
| 动作空间 | `MultiDiscrete([5, 5, 2])` |
| 宏动作长度 | `6 step` |
| 山体数量 | `24` |
| 山体半径 | `35-80px` |
| 内凹概率 | `0.92` |
| 深内凹概率 | `0.45` |
| 目标圈 | `8px`，进入后直接成功终止 |

真实动作：

| 维度 | 含义 | 档位 |
|---:|---|---|
| 1 | `move_mode` | `0 normal`, `1 back`, `2 back-left`, `3 back-right`, `4 normal fine adjust` |
| 2 | `angle_offset_bin` | `-60, -30, 0, +30, +60` |
| 3 | `speed_bin` | `50 / 100` |

真实 12 维状态：

| 维度 | 状态 | 说明 |
|---:|---|---|
| 1 | `dx_norm` | 目标相对旧坐标的 x 偏移 |
| 2 | `dy_norm` | 目标相对旧坐标的 y 偏移 |
| 3 | `sin_heading` | 当前朝向正弦 |
| 4 | `cos_heading` | 当前朝向余弦 |
| 5 | `sin_angle_error` | 目标角误差正弦 |
| 6 | `cos_angle_error` | 目标角误差余弦 |
| 7 | `coord_age_norm` | 坐标年龄 |
| 8 | `distance_progress_norm` | 最近距离进展 |
| 9 | `no_progress_time_norm` | 基于观测进展估计的无进展时间 |
| 10 | `distance_norm` | 当前距离归一化 |
| 11 | `last_move_mode` | 上一步离散移动模式 |
| 12 | `macro_phase_norm` | 当前宏 recovery 剩余阶段 |

已完成的审计项：

| 审计项 | 结果 | 证据 |
|---|---|---|
| 状态维度固定 | 通过，`obs_shape=(12,)` | `runs/目标8离散宏动作v5_本地审计/audit.csv` |
| 动作空间离散宏动作 | 通过，`MultiDiscrete([5 5 2])` | `runs/目标8离散宏动作v5_本地审计/audit.csv` |
| 山体不重叠 | 通过，5 个本地 smoke seed overlap 为 0 | `runs/目标8离散宏动作v5_本地审计/audit.csv` |
| 宏动作保持 | 通过，back recovery 连续生效，剩余步数递减 | `runs/目标8离散宏动作v5_本地审计/audit.csv` |
| 压力种子有效 | 通过，找到直线撞山种子 | `runs/目标8离散宏动作v5_本地审计/audit.csv` |
| v1002 远端结构审计 | 通过 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v5_远端审计/audit.csv` |
| v1002 训练 smoke | 通过，4 episode 能训练、评估、写结果 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v5_远端smoke/` |

v1002 长训练：

| 项 | 值 |
|---|---|
| 设备 | `v1002` |
| 环境 | `conda activate ML` |
| 目录 | `/home/diana/fishing/blind_nav_rl` |
| run dir | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v5_100k` |
| 训练轮数 | `100000` |
| 并行环境 | `128` |
| 训练日志 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v5_100k/训练.log` |
| PID 文件 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v5_100k/训练.pid` |
| checkpoint watcher | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v5_checkpoint评估/watcher.pid` |
| checkpoint 评估策略 | 只评估 `10000` 的倍数，普通 20 次 + 压力 20 次，不生成中间视频 |

查看进度：

```bash
ssh v1002 'cd /home/diana/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v5_100k/训练.log'
ssh v1002 'cd /home/diana/fishing/blind_nav_rl && find runs/目标8离散宏动作v5_100k -maxdepth 4 -type f | sort | tail -n 80'
ssh v1002 'cd /home/diana/fishing/blind_nav_rl && cat runs/目标8离散宏动作v5_checkpoint评估/checkpoint_eval_summary.csv 2>/dev/null || true'
ssh v1002 'cd /home/diana/fishing/blind_nav_rl && ps -p $(cat runs/目标8离散宏动作v5_100k/训练.pid) -o pid,stat,etime,cmd'
ssh v1002 'cd /home/diana/fishing/blind_nav_rl && ps -p $(cat runs/目标8离散宏动作v5_checkpoint评估/watcher.pid) -o pid,stat,etime,cmd'
```

最终结果：

| 项 | 值 |
|---|---:|
| 选择 checkpoint | `100000` |
| 可训练参数量 | `477325` |
| 普通成功率 | `0.90` |
| 普通平均步数 | `44.5` |
| 普通路径效率 | `0.951` |
| 普通平均角度误差 | `7.71 deg` |
| 压力成功率 | `0.75` |
| 压力平均步数 | `90.2` |
| 压力路径效率 | `0.761` |
| 压力平均角度误差 | `7.41 deg` |
| 压力平均碰撞数 | `69.8` |
| 压力脱困动作率 | `0.000` |

最终归档：

| 文件 | 位置 |
|---|---|
| 模型 | `/home/diana/screencap/file/目标8离散宏动作v5最终结果/模型.zip` |
| checkpoint 评估汇总 | `/home/diana/screencap/file/目标8离散宏动作v5最终结果/checkpoint评估汇总.csv` |
| 普通汇总指标 | `/home/diana/screencap/file/目标8离散宏动作v5最终结果/普通汇总指标.csv` |
| 普通详细指标 | `/home/diana/screencap/file/目标8离散宏动作v5最终结果/普通详细指标.csv` |
| 普通测试视频 | `/home/diana/screencap/file/目标8离散宏动作v5最终结果/普通测试视频.mp4` |
| 压力汇总指标 | `/home/diana/screencap/file/目标8离散宏动作v5最终结果/压力汇总指标.csv` |
| 压力详细指标 | `/home/diana/screencap/file/目标8离散宏动作v5最终结果/压力详细指标.csv` |
| 压力测试视频 | `/home/diana/screencap/file/目标8离散宏动作v5最终结果/压力测试视频.mp4` |
| 最终选择说明 | `/home/diana/screencap/file/目标8离散宏动作v5最终结果/最终选择说明.md` |

已验证本地链接：

- `http://127.0.0.1:6000/file/%E7%9B%AE%E6%A0%878%E7%A6%BB%E6%95%A3%E5%AE%8F%E5%8A%A8%E4%BD%9Cv5%E6%9C%80%E7%BB%88%E7%BB%93%E6%9E%9C/%E6%99%AE%E9%80%9A%E6%B5%8B%E8%AF%95%E8%A7%86%E9%A2%91.mp4`
- `http://127.0.0.1:6000/file/%E7%9B%AE%E6%A0%878%E7%A6%BB%E6%95%A3%E5%AE%8F%E5%8A%A8%E4%BD%9Cv5%E6%9C%80%E7%BB%88%E7%BB%93%E6%9E%9C/%E5%8E%8B%E5%8A%9B%E6%B5%8B%E8%AF%95%E8%A7%86%E9%A2%91.mp4`
- `http://127.0.0.1:6000/file/%E7%9B%AE%E6%A0%878%E7%A6%BB%E6%95%A3%E5%AE%8F%E5%8A%A8%E4%BD%9Cv5%E6%9C%80%E7%BB%88%E7%BB%93%E6%9E%9C/%E6%9C%80%E7%BB%88%E9%80%89%E6%8B%A9%E8%AF%B4%E6%98%8E.md`

外部分享入口状态：

- `http://ai.jiaran.icu:12346/file/目标8离散宏动作v5最终结果/普通测试视频.mp4`
- `http://ai.jiaran.icu:12346/file/目标8离散宏动作v5最终结果/压力测试视频.mp4`
- `http://ai.jiaran.icu:12346/file/目标8离散宏动作v5最终结果/最终选择说明.md`

本地 `127.0.0.1:6000` 文件路由已验证可下载。审计时外部 `ai.jiaran.icu:12346` 代理对 `/file/health.txt` 也返回 `403`，所以外部代理不可作为可访问性证据。

视频验证：

- 普通测试视频：H.264，`960x960`，`20fps`，`293.0s`。
- 压力测试视频：H.264，`960x960`，`20fps`，`567.2s`。

结论：v5 审计清单已完成闭环，但压力图的脱困动作率仍为 `0.000`，说明当前策略仍没有主动使用 recovery 宏动作；如果下一步继续优化，重点应放在 recovery 探索和奖励设计，而不是继续堆同配置训练轮数。

最终归档脚本：

```bash
ssh v1002 'cd /home/diana/fishing/blind_nav_rl && source ~/miniconda3/etc/profile.d/conda.sh && conda activate ML && python finalize_v5_audit.py --min-checkpoint 100000'
```

归档输出目录：`runs/目标8离散宏动作v5_最终归档/`。脚本会从 checkpoint 评估汇总中选择最优 checkpoint，生成普通 20 次和压力 20 次拼接视频，并整理为中文文件名。

分布式任务脚本：

```bash
python distributed_v5_launcher.py --profile light --tasks 2
python distributed_v5_launcher.py --profile normal --tasks 4 --hosts v1002 253 local
python distributed_v5_progress.py --manifest runs/分布式任务_<batch>.json
python distributed_v5_progress.py --include-main
```

默认分设备规则：

- `local`：原始门槛 `cpu<85%`、`mem<80%`；启动有效门槛 `cpu<80%`、`mem<77%`。
- `v1002`：原始门槛 `cpu<90%`、`mem<80%`；启动有效门槛 `cpu<85%`、`mem<77%`。
- `253`：原始门槛 `cpu<80%`、`mem<80%`；启动有效门槛 `cpu<75%`、`mem<77%`。
- 轻任务优先 `local -> v1002 -> 253`；普通/重任务优先 `v1002 -> 253 -> local`。
- 每个任务启动前都会重新探测目标机器资源。
- 任务目录和文件名使用中文，写在各机器项目目录的 `runs/` 下。

### 环境速度

`benchmark_env_speed.py` 最近记录：

| 模式 | step/s | ms/step | 加速 |
|---|---:|---:|---:|
| legacy full scan | 895 | 1.118 | 1.0x |
| AABB full scan | 11046 | 0.091 | 12.3x |
| spatial grid + AABB | 13891 | 0.072 | 15.5x |

固定种子和动作下，优化碰撞路径与旧 full scan 的步数、最终位置、奖励、碰撞数、成功标志一致。

## 状态与动作约束记录

当前真实部署能可靠获得：

- 自身坐标，但刷新慢，约 500ms 一次
- 目标坐标
- 当前人物/指针方向，识别很快
- 外部控制器记录的上一次摇杆角度、速度、jump

不希望依赖：

- 地图障碍物预处理
- 射线检测
- 碰撞标志
- 卡住标志
- 山体边、圆心、顶点等障碍几何

当前 8D target-circle 状态：

| # | 状态 | 含义 |
|---:|---|---|
| 1 | `dx_norm` | 目标相对上次坐标观测点的 x 偏移 |
| 2 | `dy_norm` | 目标相对上次坐标观测点的 y 偏移 |
| 3 | `sin_heading` | 当前人物方向正弦 |
| 4 | `cos_heading` | 当前人物方向余弦 |
| 5 | `sin_angle_error` | 目标方向减当前人物方向的角度差正弦 |
| 6 | `cos_angle_error` | 目标方向减当前人物方向的角度差余弦 |
| 7 | `coord_age_norm` | 坐标距现在多久 |
| 8 | `distance_progress_norm` | 最近一次坐标观测中距离减少量 |

关键动作结论：

- 不建议让 RL 学“停下”；进入 `8px` 停止圈后由环境/部署层直接松开摇杆。
- `target_relative_offset` 比 `stick_angle_delta` 更适合直线到目标。
- 如果摇杆方向和指针方向第一帧不对应，外部控制器应记录当前摇杆角度；RL 输出相对目标方向偏移或相对上一次摇杆角度增量。
- 内凹山体脱困需要后退/侧退能力，但现在连续 recovery selector 没学好。

## 已知问题

| 问题 | 当前判断 | 后续处理 |
|---|---|---|
| 蛇形回归 | `target-circle v2` 已经基本压住蛇形；后续如果换回 `stick_delta`、改成 stop 任务、加 recovery、换地图难度或换评估条件，蛇形可能重新出现 | 不是重新解决同一个问题，而是把 v2 的约束迁移到新分支并做回归检查 |
| 接近目标来回穿插 | 停止条件曾经过于复杂或模型还负责“停” | 8px 圈内直接终止，部署层直接松摇杆 |
| 内凹山体永久卡住 | 盲状态下只能靠时间序列猜，连续 recovery 不容易探索 | 改成离散/宏动作 recovery，不加障碍状态 |
| 坐标 500ms 延迟 | 决策拿到的是旧坐标和新 heading | 训练环境随机化坐标刷新间隔，状态保留 `coord_age_norm` |
| 训练慢 | 主要在环境碰撞检测和多进程开销 | 已做 spatial grid；后续减少渲染、用更小模型、多进程批量评估 |
| 并行数过大回归 | 384 env 曾经效果不如 256 | 默认保持 256，除非只是跑吞吐测试 |

## 文件和产物位置

本地项目：

- `/home/diana/fishing/blind_nav_rl`

远端训练项目：

- `/home/weiaokang/fishing/blind_nav_rl`

远端持久产物：

- `/home/weiaokang/screencap/file`

本地持久分享：

- `/home/diana/screencap/file`

临时分享：

- `/tmp/file`

注意：共享目录 `/home/weiaokang/screencap/file`、`/home/diana/screencap/file`、`/tmp/file` 下以后生成的目录名和文件名使用中文，保留扩展名。

## 后续计划

### 1. 固定一条可部署主线

以 `observable8_target8_v2_offset60` 为主线：

- 状态保持 8D，不引入地图、射线、碰撞标志。
- 停止规则固定为 `distance_to_target <= 8px` 直接成功。
- 动作保持目标方向偏移，不再回到上一摇杆角度大范围增量路线。
- 优先训练 Tiny `64x1` 和 Current `256x2`，Large 只做上限对照。

### 2. 单独解决内凹山体脱困

不加障碍信息，但把动作改得更容易学：

- 方案 A：离散 recovery 模式：`normal/back/back-left/back-right`。
- 方案 B：宏动作 recovery：一旦选择后退或侧退，环境连续保持 2-5 个 step。
- 方案 C：分层动作：普通移动由 `target_relative_offset` 控制，recovery 只在无进展时间较长时参与奖励。

评估指标必须加入：

- 压力地图成功率
- 卡住后平均恢复时间
- recovery 使用率
- recovery 后是否重新接近目标

### 3. 防止蛇形回归

目标是保留 `target-circle v2` 已经验证过的反蛇形能力，并避免新分支把问题带回来：

- 不把已解决的 v2 路线回退到 `stick_delta` 或 `stick_angle_delta`。
- 新增 recovery 或 stop 任务时，继续保留目标方向偏移、远距离角度误差惩罚、横向位移惩罚和左右翻转惩罚。
- 用直线路径效率作为 checkpoint 选择指标之一，而不是只看成功率。
- 每次改动作或奖励后都跑同一批直线无障碍/轻障碍种子，确认没有重新出现蛇形。

### 4. 模拟真实控制延迟

环境继续模拟真实部署：

- 坐标刷新间隔随机化，例如 `0.35s-0.75s`。
- 每次决策使用旧坐标 + 当前 heading。
- 状态保留 `coord_age_norm`，让模型知道坐标有多旧。
- 不强制真实设备必须等同虚拟 `dt`，但训练里要覆盖真实延迟范围。

### 5. 训练和回归流程

每次新实验都按同一套流程记录：

1. 训练 10k smoke run，确认没有明显蛇形/停不住/不动。
2. 训练 100k 主实验。
3. 普通地图 20 次评估。
4. 压力地图 20 次评估，增加内凹山体比例。
5. 生成拼接视频。
6. 把模型、CSV、视频、命令写回本文件。

### 6. 安全运行规则

- 技术验证本地跑。
- 大 CPU/内存训练用 `ssh 253`，目录 `/home/weiaokang`，环境 `source ./ML/bin/activate`。
- GPU 任务用 `ssh v10032`，目录 `/data`，环境 `conda activate ML`。
- 长期部署用 `ssh v1002`，环境 `conda activate ML`。
- 默认资源限制：内存、CPU 不超过用户指定阈值；未指定时遵守项目规范。
- 下载依赖优先镜像源。
- 不删除用户未确认的模型和产物；清理只针对明确的旧视频/临时文件。

## v6 恢复压力强化版

日期：2026-05-19

目的：在不增加地图、射线、碰撞法线等额外输入的前提下，继续提高“盲人宏脱困”的可学习性。v6 保持 v5 的 12 维状态和离散宏动作接口，只加强陷阱分布和 reward。

### 设计

| 项 | v6 |
|---|---|
| 实验名 | `rppo_medium_lstm128x2_observable12_target8_discrete_macro_v6` |
| 状态 | `observable12_target8_discrete_macro_v6` |
| 参数量 | `477325` |
| 动作空间 | `MultiDiscrete([5, 5, 2])` |
| 宏动作长度 | `6 step` |
| 山体数量 | `28` |
| 山体半径 | `35-85px` |
| 内凹概率 | `0.96` |
| 深内凹概率 | `0.60` |
| reward profile | `target_line_macro_recovery_v6` |

v6 reward 加强点：

- `no_progress_time` 已经高了还继续 normal：额外惩罚
- 刚碰撞/刚脱离碰撞但仍不改变策略：额外惩罚
- recovery 让 progress 恢复：额外奖励
- `prev_stuck + recovery + 明显前进`：再加一层奖励
- recovery 仍然明显负进展：轻微惩罚

### 已完成验证

| 项 | 结果 | 证据 |
|---|---|---|
| 本地编译检查 | 通过 | `py_compile` |
| 本地审计 | 通过 | `runs/目标8离散宏动作v6_本地审计/audit.csv` |
| 253 远端审计 | 通过 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v6_远端审计/audit.csv` |
| 253 远端 smoke 训练 | 通过 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v6_远端smoke/` |
| 253 远端 pressure smoke | 通过 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v6_远端smoke_pressure/` |

### 253 smoke 指标

| 指标 | 值 |
|---|---:|
| 训练轮数 | `256` |
| 训练耗时 | `41.19s` |
| 普通成功率 | `1.00` |
| 普通平均步数 | `42.5` |
| 压力测试次数 | `4` |
| 压力成功率 | `0.50` |
| 压力平均步数 | `150.75` |
| 压力平均终点距离 | `298.95` |
| 压力平均角度误差 | `3.19 deg` |
| 压力平均碰撞数 | `126.25` |
| 压力脱困动作率 | `0.000` |

### 当前结论

- v6 代码路径、训练路径、压力评估路径都已经打通。
- 短 smoke 训练还没有学出显式宏脱困动作。
- 下一步应做长训练和 checkpoint 跟踪，不需要继续加本地 smoke。

### v6 长训练已启动

当前运行位置：

| 项 | 值 |
|---|---|
| 设备 | `253` |

## v7 两段式 heading-relative 宏脱困

日期：2026-05-19

目的：`v6` 在 30k checkpoint 已经表现出明显平台，说明单纯增加 pressure reward 还不够。`v7` 不再继续只调 reward，而是直接把 recovery 原语改成更接近真实“后退脱坑”的动作模板。

### 设计

| 项 | v7 |
|---|---|
| 实验名 | `rppo_medium_lstm128x2_observable12_target8_discrete_macro_v7` |
| 状态 | `observable12_target8_discrete_macro_v7` |
| 参数量 | `477325` |
| 动作空间 | `MultiDiscrete([5, 5, 2])` |
| 正常动作角度桶 | `[-60, -20, 0, 20, 60] deg` |
| 宏动作长度 | `6 step` |
| 脱困参考系 | 相对当前 `heading` |
| 宏动作结构 | 前半程后退，后半程横向脱出 |
| 山体数量 | `32` |
| 山体半径 | `35-85px` |
| 内凹概率 | `0.98` |
| 深内凹概率 | `0.72` |
| reward profile | `target_line_macro_recovery_v7` |

### 与 v6 的核心区别

- `v6` 的 recovery 是相对目标方向
- `v7` 的 recovery 是相对当前 heading
- `v6` 的 recovery 是单段固定方向
- `v7` 的 recovery 是两段式：先退，再左/右横移
- `v7` 把 0 附近的 steering 分辨率加密，降低蛇形风险

### 已完成验证

| 项 | 结果 | 证据 |
|---|---|---|
| 本地编译检查 | 通过 | `py_compile` |
| 本地审计 | 通过 | `runs/目标8离散宏动作v7_本地审计/audit.csv` |

### 当前结论

- `v7` 已经具备独立训练入口、独立 pressure eval 入口、独立审计入口。
- `v6` 的旧模型、旧表格、旧脚本没有被覆盖。
- 下一步应该跑本地 smoke，然后上 `253` 做真正的长训练。

### 253 长训练已启动

当前运行位置：

| 项 | 值 |
|---|---|
| 设备 | `253` |
| 训练目录 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v7_100k` |
| checkpoint 评估目录 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v7_checkpoint评估` |
| 最终归档目录 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v7_最终归档` |
| 训练 PID | `4190737` |
| watcher PID | `4190739` |
| finalizer PID | `4190742` |
| 训练配置 | `EPISODES=100000`, `N_ENVS=256`, `THREADS=1` |

查看命令：

- `rtk ssh 253 'cd /home/weiaokang/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v7_100k/训练.log'`
- `rtk ssh 253 'cd /home/weiaokang/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v7_checkpoint评估/watcher.log'`
- `rtk python3 distributed_v7_progress.py --hosts 253 --include-main`

### v1002 并行长训练已启动

当前运行位置：

| 项 | 值 |
|---|---|
| 设备 | `v1002` |
| 训练目录 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v7_v1002_100k` |
| checkpoint 评估目录 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v7_v1002_checkpoint评估` |
| 最终归档目录 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v7_v1002_最终归档` |
| 训练 PID | `993496` |
| watcher PID | `993498` |
| finalizer PID | `993501` |
| 训练配置 | `EPISODES=100000`, `N_ENVS=128`, `THREADS=1` |

查看命令：

- `rtk ssh v1002 'cd /home/diana/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v7_v1002_100k/训练.log'`
- `rtk ssh v1002 'cd /home/diana/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v7_v1002_checkpoint评估/watcher.log'`
- `rtk python3 distributed_v7_progress.py --hosts 253 v1002 --include-main`

### v7 早期手工评估

日期：2026-05-19

在 `253` 主线训练还没到 `10k checkpoint` 前，先对 `checkpoint_ep_02000.zip` 做了手工 normal/pressure 评估，避免盲等。

| checkpoint | 普通成功率 | 压力成功率 | 普通平均角度误差 | 压力平均角度误差 | 压力平均碰撞数 | recovery rate |
|---:|---:|---:|---:|---:|---:|---:|
| 2000 | `0.75` | `0.65` | `3.09 deg` | `2.18 deg` | `86.5` | `0.000` |

当前解释：

- `v7` 在很早期就有一定普通/压力通过率，说明基础控制没坏。
- 但 `recovery_rate` 仍然是 `0.000`，说明模型此时还没有学会显式触发新的两段式脱困动作。
- 后续仍要看 `10k / 20k / 30k` checkpoint，不能只用 `2k` 下结论。

额外修复：

- 修了 `eval_target8_pressure.py` 的 CLI 参数 bug。之前内部逻辑已经支持 `v7`，但 `argparse choices` 没把 `v7` 加进去；如果不修，watcher 到首个自动 pressure 评估时会直接失败。

### v7 到 10k 的阶段结论

日期：2026-05-19

`253` 主线已经跑到 `checkpoint 10000`，并且手工/自动评估链路都已修通。当前结论不是“还没到 checkpoint”，而是：

| checkpoint | 普通成功率 | 压力成功率 | 压力平均碰撞数 | recovery rate |
|---:|---:|---:|---:|---:|
| 2000 | `0.75` | `0.65` | `86.5` | `0.000` |
| 5000 | `0.75` | `0.65` | `86.5` | `0.000` |
| 10000 | `0.75` | `0.65` | `86.5` | `0.000` |

阶段判断：

- `v7` 不是单纯训练不够稳定，而是目前已经表现出明显平台。
- 到 `10k` 为止，模型没有学出显式两段式脱困动作。
- 下一步不应继续只等更长训练，而应开新的 `v8` 分支，直接加强“受困起点 / 内凹陷阱暴露”的训练分布。

## v8 受困起点分布

日期：2026-05-19

目的：`v7` 的动作原语已经换过，但训练分布仍然偏“先自由走，再偶尔卡住”。`v8` 保持 blind 观测和相同动作语义不变，直接在环境采样里加入“起点已受困”的 episode。

### 设计

| 项 | v8 |
|---|---|
| 实验名 | `rppo_medium_lstm128x2_observable12_target8_discrete_macro_v8` |
| 状态 | `observable12_target8_discrete_macro_v8` |
| 动作 | 与 `v7` 相同 |
| reward | 与 `v7` 相同 |
| 新增环境参数 | `trapped_start_probability=0.55` |
| 山体数量 | `34` |
| 内凹概率 | `0.99` |
| 深内凹概率 | `0.82` |

### 本地验证

| 项 | 结果 | 证据 |
|---|---|---|
| 本地编译检查 | 通过 | `py_compile` |
| 本地审计 | 通过 | `runs/目标8离散宏动作v8_本地审计/audit.csv` |
| 本地 smoke | 通过 | `runs/目标8离散宏动作v8_本地smoke2/` |

补充说明：

- 本地 8 个审计 seed 里，有 `3` 个 seed 在 reset 后已经处于近山受困状态。
- 这说明 `v8` 的新分布已经开始生效，但不是每个 episode 都强制受困；这是预期行为，因为还需要保留一部分普通样本。

### v1002 上的 v8 长训练已启动

| 项 | 值 |
|---|---|
| 设备 | `v1002` |
| 训练目录 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v8_v1002_100k` |
| checkpoint 评估目录 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v8_v1002_checkpoint评估` |
| 最终归档目录 | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v8_v1002_最终归档` |
| 训练 PID | `1160168` |
| watcher PID | `1160170` |
| finalizer PID | `1160173` |
| 训练配置 | `EPISODES=100000`, `N_ENVS=128`, `THREADS=1` |

查看命令：

- `rtk ssh v1002 'cd /home/diana/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v8_v1002_100k/训练.log'`
- `rtk ssh v1002 'cd /home/diana/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v8_v1002_checkpoint评估/watcher.log'`
- `rtk python3 distributed_v8_progress.py`

### 253 上的 v8 并行长训练已启动

| 项 | 值 |
|---|---|
| 设备 | `253` |
| 训练目录 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v8_100k` |
| checkpoint 评估目录 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v8_checkpoint评估` |
| 最终归档目录 | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v8_最终归档` |
| 训练 PID | `12078` |
| watcher PID | `12080` |
| finalizer PID | `12083` |
| 训练配置 | `EPISODES=100000`, `N_ENVS=128`, `THREADS=1` |

查看命令：

- `rtk ssh 253 'cd /home/weiaokang/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v8_100k/训练.log'`
- `rtk ssh 253 'cd /home/weiaokang/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v8_checkpoint评估/watcher.log'`
- `rtk python3 distributed_v8_progress.py`
| 环境 | `source /home/weiaokang/ML/bin/activate` |
| 目录 | `/home/weiaokang/fishing/blind_nav_rl` |
| run dir | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v6_100k` |
| checkpoint eval dir | `/home/weiaokang/fishing/blind_nav_rl/runs/目标8离散宏动作v6_checkpoint评估` |
| 训练轮数 | `100000` |
| 并行环境 | `128` |
| train pid | `4165554` |
| watcher pid | `4166108` |

查看命令：

```bash
rtk ssh 253 'cd /home/weiaokang/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v6_100k/训练.log'
rtk ssh 253 'cd /home/weiaokang/fishing/blind_nav_rl && ps -p $(cat runs/目标8离散宏动作v6_100k/训练.pid) -o pid,stat,etime,cmd'
rtk ssh 253 'cd /home/weiaokang/fishing/blind_nav_rl && ps -p $(cat runs/目标8离散宏动作v6_checkpoint评估/watcher.pid) -o pid,stat,etime,cmd'
rtk ssh 253 'cd /home/weiaokang/fishing/blind_nav_rl && cat runs/目标8离散宏动作v6_checkpoint评估/checkpoint_eval_summary.csv 2>/dev/null || true'
rtk python3 distributed_v6_progress.py --hosts 253 v1002 --include-main
```

新增配套脚本：

- `watch_v6_checkpoints.py`
- `finalize_v6_audit.py`
- `launch_v6_finalizer.sh`
- `distributed_v6_launcher.py`
- `distributed_v6_progress.py`
- `checkpoint_summary.py`
- `append_manual_checkpoint_eval.py`

### 253 首个 checkpoint 结果

当前 `253` 已跑到 `checkpoint=10000`，并完成首轮普通/压力评估。

| 指标 | 值 |
|---|---:|
| checkpoint | `10000` |
| 普通成功率 | `0.80` |
| 普通平均步数 | `68.6` |
| 普通路径效率 | `0.9229` |
| 普通平均角度误差 | `6.49 deg` |
| 压力成功率 | `0.80` |
| 压力平均步数 | `78.15` |
| 压力路径效率 | `0.8933` |
| 压力平均角度误差 | `6.96 deg` |
| 压力脱困动作率 | `0.000` |
| 压力平均碰撞数 | `56.45` |

结论：

- v6 在 `10000` checkpoint 时，普通和压力成功率都已经到 `0.80`。
- 但显式宏脱困动作率仍然是 `0.000`，说明“会绕开一部分复杂障碍”和“学会主动用 recovery”仍不是一回事。
- `checkpoint_summary.py` 已接入，可直接比较 first/latest/best checkpoint，后续盯 `20000`、`30000` 不需要手工读 CSV。

### 253 手动复核 20000 checkpoint

因为 watcher 还没把 `20000` 写进 `checkpoint_eval_summary.csv`，单独对 `checkpoint_ep_20000.zip` 做了一次手动普通/压力评估：

| 指标 | `10000` | `20000` 手动评估 |
|---|---:|---:|
| 普通成功率 | `0.80` | `0.80` |
| 普通平均步数 | `68.6` | `68.6` |
| 普通路径效率 | `0.9229` | `0.9229` |
| 普通平均角度误差 | `6.49 deg` | `6.49 deg` |
| 压力成功率 | `0.80` | `0.80` |
| 压力平均步数 | `78.15` | `78.15` |
| 压力路径效率 | `0.8933` | `0.8933` |
| 压力平均角度误差 | `6.96 deg` | `6.96 deg` |
| 压力脱困动作率 | `0.000` | `0.000` |
| 压力平均碰撞数 | `56.45` | `56.45` |

当前判断：

- 在固定评估种子上，`20000` 相比 `10000` 没有任何可观测改进。
- 至少到 `20000` 为止，v6 还没有学出显式 recovery 行为。
- 手动 `20000` 结果已补入 `checkpoint_eval_summary.csv`，后续 `checkpoint_summary.py` / finalizer 都能直接看到它。
- watcher 之后已经恢复正常写入，`checkpoint_eval_summary.csv` 目前包含 `10000` 和 `20000` 两行。
- 实验含义已经比较明确：继续单纯拉长训练，不足以让 v6 在前 20000 轮内学出 recovery。

### 253 手动复核 30000 checkpoint

继续对 `checkpoint_ep_30000.zip` 做了一次手动普通/压力评估，并补入 `checkpoint_eval_summary.csv`。

| 指标 | `10000` | `20000` | `30000` 手动评估 |
|---|---:|---:|---:|
| 普通成功率 | `0.80` | `0.80` | `0.80` |
| 普通平均步数 | `68.6` | `68.6` | `68.6` |
| 普通路径效率 | `0.9229` | `0.9229` | `0.9229` |
| 普通平均角度误差 | `6.49 deg` | `6.49 deg` | `6.49 deg` |
| 压力成功率 | `0.80` | `0.80` | `0.80` |
| 压力平均步数 | `78.15` | `78.15` | `78.15` |
| 压力路径效率 | `0.8933` | `0.8933` | `0.8933` |
| 压力平均角度误差 | `6.96 deg` | `6.96 deg` | `6.96 deg` |
| 压力脱困动作率 | `0.000` | `0.000` | `0.000` |
| 压力平均碰撞数 | `56.45` | `56.45` | `56.45` |

结论进一步收紧：

- 在固定评估种子上，`10000 -> 20000 -> 30000` 完全一致。
- 至少前 `30000` 轮内，继续拉长训练没有让 v6 学出显式 recovery。

### v1002 并行长训练已启动

为了增加有效尝试数，又补起了一条不同 seed 的并行 v6 长训练：

| 项 | 值 |
|---|---|
| 设备 | `v1002` |
| 环境 | `conda activate ML` |
| run dir | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v6_v1002_100k` |
| checkpoint eval dir | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v6_v1002_checkpoint评估` |
| final archive dir | `/home/diana/fishing/blind_nav_rl/runs/目标8离散宏动作v6_v1002_最终归档` |
| 训练轮数 | `100000` |
| 并行环境 | `128` |
| seed base | `9600000` |
| eval seed base | `9700000` |

当前状态：

- `v1002` 训练进程在跑。
- `watch_v6_checkpoints.py` 已重新挂稳。
- `launch_v6_finalizer.sh` 已在等待最终 checkpoint 评估。

查看命令：

```bash
rtk ssh v1002 'cd /home/diana/fishing/blind_nav_rl && tail -f runs/目标8离散宏动作v6_v1002_100k/训练.log'
rtk ssh v1002 'cd /home/diana/fishing/blind_nav_rl && ps -p $(cat runs/目标8离散宏动作v6_v1002_100k/训练.pid) -o pid,stat,etime,cmd'
rtk ssh v1002 'cd /home/diana/fishing/blind_nav_rl && cat runs/目标8离散宏动作v6_v1002_checkpoint评估/checkpoint_eval_summary.csv 2>/dev/null || true'
```

## v11 宏动作库短训与对角高障碍评估

目的：针对“遇到障碍转圈/顶住不走”的问题，把 recovery 从单一脱困开关扩展成可选宏动作库，让策略选择预设脱困模板。

实现状态：

- 新增环境动作模式：`heading_relative_macro_library_recovery`。
- 离散 wrapper 动作空间：`MultiDiscrete([5, 2, 7])`，第三维是 7 个宏动作 ID。
- 宏动作模板：`normal`、`short_back`、`back_left`、`back_right`、`wide_left`、`wide_right`、`back_then_realign`。
- 训练脚本：`train_target8_v11_macro_library_curriculum.py`。
- 远端短训目录：`/home/diana/fishing/blind_nav_rl/runs/目标8宏动作库v11短训_v1002`。

验证：

- 本地和 `v1002` 远端 `py_compile` 通过。
- v11 四个阶段环境 smoke 通过，观测维度为 `12`，动作空间为 `MultiDiscrete([5 2 7])`。
- 强制 gate smoke 能触发宏动作 ID `1..6`，说明环境侧宏动作执行链路有效。

短训阶段内评估：

| 阶段 | 训练中 eval 成功率 |
|---|---:|
| stage1 | `18/20` |
| stage2a | `16/20` |
| stage2 | `19/20` |
| stage3 | `18/20` |

固定评估结果：

| 场景 | 模型 | 成功率 | 平均步数 | 路径效率 | 到达角误差 | recovery 率 | 平均碰撞 |
|---|---|---:|---:|---:|---:|---:|---:|
| normal | v11 stage2 | `0.90` | `42.20` | `0.8661` | `0.060 deg` | `0.000` | `27.35` |
| normal | v11 stage3 | `0.85` | `60.75` | `0.8788` | `0.050 deg` | `0.000` | `44.30` |
| pressure | v11 stage2 | `0.90` | `47.00` | `0.9426` | `0.00005 deg` | `0.000` | `31.35` |
| pressure | v11 stage3 | `0.90` | `47.00` | `0.9426` | `0.00005 deg` | `0.000` | `31.35` |
| 对角高障碍 | v11 stage2 | `0.55` | `148.90` | `0.8690` | `0.232 deg` | `0.000` | `108.35` |
| 对角高障碍 | v11 stage3 | `0.55` | `157.90` | `0.8671` | `0.232 deg` | `0.000` | `117.35` |
| 对角高障碍 | AE500 v10 stage3 | `0.75` | `167.40` | `0.3953` | `19.921 deg` | `0.4458` | `25.05` |

视频：

- v11 stage2 对角高障碍 20 轮拼接：`http://ai.jiaran.icu:12346/file/%E7%9B%AE%E6%A0%878%E5%AE%8F%E5%8A%A8%E4%BD%9C%E5%BA%93v11%E8%AF%84%E4%BC%B0/v11_stage2%E5%AF%B9%E8%A7%92%E9%AB%98%E9%9A%9C%E7%A2%8D20%E8%BD%AE%E6%8B%BC%E6%8E%A5.mp4`
- URL 本地探测：`200`，下载大小约 `5.8 MB`。

结论：

- v11 的“宏动作库动作空间”已经实现并可执行，但短训策略没有学会主动触发宏脱困，所有固定评估 recovery 率都是 `0.000`。
- v11 stage2 比 stage3 更适合作为当前候选：normal 分数更高，对角高障碍碰撞更少。
- v11 stage2 的成功轨迹更直、到达角误差更低，但高障碍成功率低于 AE500，失败时仍会顶障碍/卡住。
- 下一步不应只继续拉长同一训练；需要给宏动作增加可学习信号，例如碰撞后强制探索宏动作、宏动作使用先验/行为克隆、或 recovery 触发的辅助奖励，否则策略会继续忽略第三维宏动作。

### 2026-05-21 摆烂/原地踏步指标补充

为区分“策略主动少动”和“想动但碰撞后被退回原地”，给以下评估脚本新增了统一指标：

- `eval_target8_sweep.py`
- `eval_target8_pressure.py`
- `eval_target8_diagonal_dense_video.py`

新增指标：

- `still_step_count / still_step_rate`
- `collision_still_step_count / collision_still_step_rate`
- `max_still_run`

含义：

- `still_step_*`：位置几乎不变的步数/比例
- `collision_still_step_*`：同时伴随碰撞的原地步数/比例
- `max_still_run`：最长连续原地段

对比评估目录：

- `/home/diana/fishing/blind_nav_rl/runs/摆烂指标对比_v1002`

关键结果：

| 场景 | 模型 | 成功率 | recovery 率 | 平均碰撞 | 原地步率 | 碰撞原地步率 | 最长连续原地 |
|---|---|---:|---:|---:|---:|---:|---:|
| pressure | v11 stage2 | `0.90` | `0.000` | `31.35` | `0.242` | `0.242` | `27.4` |
| pressure | AE500 | `0.95` | `0.307` | `9.10` | `0.101` | `0.101` | `2.8` |
| 对角高障碍 | v11 stage2 | `0.55` | `0.000` | `108.35` | `0.307` | `0.307` | `79.65` |
| 对角高障碍 | AE500 | `0.75` | `0.446` | `25.05` | `0.0277` | `0.0277` | `3.75` |

解释：

- `v11` 的原地步率和碰撞原地步率完全相同，说明“看起来不动”基本不是主动停，而是每次想推进都被碰撞解算退回原位。
- `v11` 在对角高障碍里的 `max_still_run ≈ 79.65`，已经接近长时间连续卡死。
- `AE500` 虽然 recovery 更多、轨迹更曲折，但它显著减少了“连续原地卡住”，说明显式 recovery 至少在“避免长时间顶障碍发呆”上是有效的。

当前判断进一步收紧：

- “一动不动”不是 reward 直接鼓励摆烂，而是 `v11` 没学会用宏脱困后，退化成了“持续撞障碍并被原地回退”的坏解。
- 继续加训练轮数大概率只会重复这一坏解。
- 下一步如果继续推进 `v11`，目标不应再是单纯提高成功率或压角，而应直接优化：
  - `collision_still_step_rate`
  - `max_still_run`
  - `recovery_action_rate`

### 2026-05-21 v11b 卡住宏动作探索短训

改动：

- 在 `Observable12MacroLibraryV11Wrapper` 里新增 stuck-context 宏动作探索注入。
- 当策略仍输出 `macro_bin=0`，但 `no_progress_time` 或 recent collision 已进入阈值区间时，以可配置概率把该步替换成非零宏动作。
- 新参数：
  - `stuck_macro_explore_prob`
  - `stuck_macro_no_progress_threshold`
  - `stuck_macro_collision_threshold`
  - `stuck_macro_choices`

配置与脚本：

- 配置：`configs/v11/方案B_卡住宏动作探索.json`
- 启动脚本：`tools/启动_v11b卡住宏动作探索短训_v1002.sh`
- 训练目录：`/home/diana/fishing/blind_nav_rl/runs/目标8宏动作库v11b卡住探索短训_v1002`

实现验证：

- 本地 `py_compile` 通过。
- `v1002` 远端 `py_compile` 通过。
- 远端 smoke：在 stuck 上下文且策略动作仍为 `macro_bin=0` 时，wrapper 已成功注入非零宏动作。

训练内阶段结果：

| 阶段 | 成功率 | 平均步数 | 平均终点距离 | 平均碰撞 |
|---|---:|---:|---:|---:|
| stage1 | `0.95` | `30.30` | `36.69` | `11.60` |
| stage2a | `1.00` | `20.25` | `8.00` | `1.50` |
| stage2 | `0.95` | `29.95` | `22.55` | `14.55` |
| stage3 | `0.90` | `47.65` | `33.97` | `30.50` |

固定种子复评：

| 场景 | 模型 | 成功率 | 平均步数 | recovery 率 | 平均碰撞 | 原地步率 | 碰撞原地步率 | 最长连续原地 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| pressure | 旧 v11 stage2 | `0.90` | `47.00` | `0.000` | `31.35` | `0.242` | `0.242` | `27.40` |
| pressure | v11b stage2 | `1.00` | `31.55` | `0.166` | `8.15` | `0.126` | `0.126` | `3.65` |
| pressure | v11b stage3 | `1.00` | `32.65` | `0.189` | `8.10` | `0.112` | `0.112` | `3.50` |
| pressure | AE500 | `0.95` | `41.55` | `0.307` | `9.10` | `0.101` | `0.101` | `2.80` |
| 对角高障碍 | 旧 v11 stage2 | `0.55` | `148.90` | `0.000` | `108.35` | `0.307` | `0.307` | `79.65` |
| 对角高障碍 | v11b stage2 | `1.00` | `75.70` | `0.154` | `12.90` | `0.0386` | `0.0386` | `2.45` |
| 对角高障碍 | v11b stage3 | `1.00` | `82.65` | `0.195` | `14.30` | `0.0326` | `0.0326` | `2.15` |
| 对角高障碍 | AE500 | `0.75` | `167.40` | `0.446` | `25.05` | `0.0277` | `0.0277` | `3.75` |

结论：

- `v11b` 已经实质性修复了旧 `v11` 的“长时间原地卡死”坏解。
- 在固定对角高障碍 20 局上，`v11b stage2/stage3` 都达到 `20/20` 成功，显著超过旧 `v11` 的 `11/20` 和 AE500 的 `15/20`。
- `v11b` 的 recovery 使用率已恢复到非零，说明“卡住时尝试宏脱困”这件事被真正学到了。
- `v11b stage2` 综合更适合作为当前主候选：
  - 对角高障碍步数更短、碰撞更少；
  - pressure 成功率 `1.00`；
  - 虽然 `stage3` 的原地步率略低，但整体导航质量和碰撞略差。
- 与 AE500 相比，`v11b stage2` 在高障碍成功率、步数、碰撞、最长连续原地上都更优；但 AE500 的 recovery 率仍更高，说明它更“激进”地在用 recovery。

### 2026-05-21 v11b vs AE500 大样本复评

复评目录：

- `/home/diana/fishing/blind_nav_rl/runs/v11b_AE500_大样本复评_v1002`

口径：

- `pressure`：`100` 局，固定 `seed_start=5200000`
- `对角高障碍`：`100` 局，固定 `seed_start=6300000`
- 两者都使用新增的 stall 指标

结果：

| 场景 | 模型 | 成功率 | 平均步数 | recovery 率 | 平均碰撞 | 原地步率 | 最长连续原地 |
|---|---|---:|---:|---:|---:|---:|---:|
| pressure | v11b stage2 | `1.00` | `32.07` | `0.192` | `9.34` | `0.149` | `4.14` |
| pressure | AE500 | `0.91` | `60.36` | `0.371` | `11.78` | `0.114` | `3.64` |
| 对角高障碍 | v11b stage2 | `0.97` | `82.51` | `0.154` | `20.76` | `0.0591` | `6.57` |
| 对角高障碍 | AE500 | `0.77` | `168.28` | `0.446` | `23.22` | `0.0229` | `2.89` |

更新后的判断：

- `v11b stage2` 在大样本上依然显著优于 AE500，说明 20 局结果不是偶然。
- 尤其在成功率和步数上，`v11b stage2` 已经建立了明确优势：
  - pressure `1.00 vs 0.91`
  - 对角高障碍 `0.97 vs 0.77`
  - 对角高障碍平均步数 `82.51 vs 168.28`
- 但 `v11b stage2` 仍不是全维度碾压：
  - 它的 `still_step_rate` 和 `max_still_run` 虽已远好于旧 v11，却仍高于 AE500
  - 说明它虽然更容易成功脱困，但一旦陷入复杂局部几何，仍可能比 AE500 更长时间地局部贴障

当前结论：

- 如果目标是“尽量少摆烂并尽量高成功率”，当前默认候选应升级为 `v11b stage2`。
- 如果目标是“尽量保持 recovery 行为激进、最长卡顿更短”，AE500 仍有参考价值，但它整体成功率和步数已经明显落后。

### 2026-05-21 v11b stage2 对角高障碍视频

生成目录：

- 远端：`/home/diana/fishing/blind_nav_rl/runs/目标8宏动作库v11b对角高障碍视频_v1002`
- 本地共享：`/home/diana/screencap/file/目标8宏动作库v11b成果`

共享产物：

- 视频：`http://ai.jiaran.icu:12346/file/%E7%9B%AE%E6%A0%878%E5%AE%8F%E5%8A%A8%E4%BD%9C%E5%BA%93v11b%E6%88%90%E6%9E%9C/v11b_stage2%E5%AF%B9%E8%A7%92%E9%AB%98%E9%9A%9C%E7%A2%8D20%E8%BD%AE%E6%8B%BC%E6%8E%A5.mp4`
- 汇总 CSV：`v11b_stage2对角高障碍汇总.csv`
- 明细 CSV：`v11b_stage2对角高障碍明细.csv`

校验：

- 本地文件路由探测返回 `200`
- 下载大小约 `3.5 MB`

对应 20 局汇总：

- success rate：`1.00`
- avg steps：`75.70`
- avg collision count：`12.90`
- avg recovery action rate：`0.154`
- avg still step rate：`0.0386`
- avg max still run：`2.45`

### 2026-05-21 v11b checkpoint watcher 修复与短训收口

背景：

- 为了把 `v11b` 真正接成默认工作流，而不是只停留在本地代码默认值，补了 `watch_v11_checkpoints.py` 和 `tools/启动_v11b卡住宏动作探索评估_v1002.sh`。
- 首次在 `v1002` 起 watcher 时，暴露出两个运维层问题：
  - 远端 `blind_nav_rl/env.py` / 评估脚本版本过旧，`v11` 宏动作 4 维 env action 无法对上，普通评估直接报 `(4,) vs (3,)` 广播错误。
  - `watch_v11_checkpoints.py` 沿用了长课程默认阈值 `20k/10k/30k/50k`，而这次短训实际是 `2000/1000/2000/1000`，导致即使 checkpoint 已全评完，watcher 也不会退出。

处理：

- 同步以下文件到 `v1002`：
  - `blind_nav_rl/env.py`
  - `benchmark_state_dims_10k.py`
  - `eval_target8_sweep.py`
  - `eval_target8_pressure.py`
  - `eval_target8_diagonal_dense_video.py`
  - `watch_v11_checkpoints.py`
- 给 `watch_v11_checkpoints.py` 增加短训用阶段上限参数：
  - `--stage1-max-checkpoint`
  - `--stage2a-max-checkpoint`
  - `--stage2-max-checkpoint`
  - `--stage3-max-checkpoint`
- 更新 `tools/启动_v11b卡住宏动作探索评估_v1002.sh`，默认传入：
  - `stage1=2000`
  - `stage2a=1000`
  - `stage2=2000`
  - `stage3=1000`
- 新增一键同步+启动脚本：
  - `tools/同步并启动_v11b卡住宏动作探索评估_v1002.sh`

验证：

- 本地 `py_compile` 通过。
- 远端 `py_compile` 通过。
- 前台手动跑 `watch_v11_checkpoints.py`，已成功写出 `checkpoint_eval_summary.csv`。
- 在已有 summary 的情况下，用短训阈值重新执行 watcher，能直接 `EXIT:0`，证明不会再空转。

`v1002` watcher 汇总：

- 目录：`/home/diana/fishing/blind_nav_rl/runs/目标8宏动作库v11b_checkpoint评估_v1002`
- 已完成到：
  - `stage1 00500/01000/01500/02000`
  - `stage2a 00500/01000`
  - `stage2 00500/01000/01500/02000`
  - `stage3 00500/01000`

关键 checkpoint 结果（20 局普通 + 20 局 pressure）：

| 阶段 | checkpoint | 普通成功率 | 普通步数 | 普通终点角度 | 普通 recovery 率 | pressure 成功率 | pressure 步数 | pressure 终点角度 | pressure recovery 率 | pressure 碰撞 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| stage2 | `00500` | `0.95` | `31.25` | `5.82deg` | `0.150` | `1.00` | `33.45` | `0.00005deg` | `0.166` | `8.15` |
| stage2 | `01000` | `1.00` | `19.90` | `0.00006deg` | `0.0587` | `1.00` | `31.55` | `0.00006deg` | `0.166` | `8.15` |
| stage2 | `01500` | `1.00` | `19.90` | `0.00006deg` | `0.0587` | `1.00` | `31.55` | `0.00006deg` | `0.166` | `8.15` |
| stage2 | `02000` | `1.00` | `19.90` | `0.00006deg` | `0.0587` | `1.00` | `31.55` | `0.00006deg` | `0.166` | `8.15` |
| stage3 | `00500` | `1.00` | `28.40` | `0.00007deg` | `0.176` | `1.00` | `32.65` | `0.00006deg` | `0.189` | `8.10` |
| stage3 | `01000` | `1.00` | `28.40` | `0.00007deg` | `0.176` | `1.00` | `32.65` | `0.00006deg` | `0.189` | `8.10` |

补充判断：

- 这轮 checkpoint watcher 与之前手动复评结论一致：当前最值得保留的仍然是 `v11b stage2`，不是 `stage3`。
- `stage2 01000` 之后指标基本不再变化，说明这次短训的有效窗口已经很短，继续在同一配置下拉长训练价值不高。
- `stage3` 虽然 recovery 率更高，但普通步数明显更差，pressure 也没有比 `stage2` 更优，不应升格为默认主线。
